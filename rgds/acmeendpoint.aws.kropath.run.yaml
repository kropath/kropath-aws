# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# ACMEEndpoint RGD — wraps ACK acm.services.k8s.aws/AcmeEndpoint.
# Naming exemption (KRO-236): ACME endpoints are identified by ARN, not name.
# The aws.kropath.run/resource-name label is applied to the ACK CR so that
# ACMEDomainValidation instances can reference this endpoint via acmeEndpointRef.
# Spec: kropath-core/docs/specs/aws/aws-acm-05-acmeendpoint.md
---
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: acmeendpoint.aws.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: aws.kropath.run
    kind: ACMEEndpoint
    scope: Namespaced
    spec:
      # Governance
      configRef: string | default=general-policy
      deletionPolicy: string | default=retain
      tags: map[string]string | default={}
      syncedLabels: map[string]string | default={}
      syncedAnnotations: map[string]string | default={}
      # ACME endpoint configuration
      authorizationBehavior: string | default=""
      allowedKeyAlgorithms: "[]string | default=[]"
      contact: string | default=""
      certificateTags: "[]ACMECertificateTag | default=[]"
    types:
      ACMECertificateTag:
        key: string | default=""
        value: string | default=""
    status:
      conditions: >-
        ${ackAcmeEndpoint.?status.?conditions.orValue([])}
      acmeEndpointARN: >-
        ${ackAcmeEndpoint.?status.?acmeEndpointARN.orValue("")}
      endpointURL: >-
        ${ackAcmeEndpoint.?status.?endpointURL.orValue("")}
      endpointStatus: >-
        ${ackAcmeEndpoint.?status.?status.orValue("")}
      missingAuthorizationBehaviorError: >-
        ${has(missingAuthorizationBehaviorError.data) ? missingAuthorizationBehaviorError.data.error : ""}
    additionalPrinterColumns:
      - name: EndpointARN
        type: string
        jsonPath: .status.acmeEndpointARN
      - name: EndpointURL
        type: string
        jsonPath: .status.endpointURL

  resources:
    # Governance config lookup.
    - id: rsrcCfg
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: ACMConfig
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: ${schema.?spec.?configRef.orValue("general-policy")}

    # Advisory ConfigMap: authorizationBehavior is required.
    # x-kubernetes-validations are discarded by kro v0.9.2 — use in-graph ConfigMap pattern.
    - id: missingAuthorizationBehaviorError
      includeWhen:
        - >-
          ${schema.spec.authorizationBehavior == ""}
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.metadata.name}-missing-authorization-behavior-error
          namespace: ${schema.metadata.namespace}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: ACMEEndpoint
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        data:
          error: "authorizationBehavior is required"

    # ACK AcmeEndpoint CR — only when authorizationBehavior is provided.
    # Carries aws.kropath.run/resource-name label so ACMEDomainValidation can find it
    # via selector.matchLabels.
    - id: ackAcmeEndpoint
      includeWhen:
        - >-
          ${schema.spec.authorizationBehavior != ""}
      template:
        apiVersion: acm.services.k8s.aws/v1alpha1
        kind: AcmeEndpoint
        metadata:
          name: ${schema.metadata.name}
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name, "aws.kropath.run/resource-name": schema.metadata.name}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: ACMEEndpoint
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          authorizationBehavior: "${schema.spec.authorizationBehavior}"
          certificateAuthority:
            publicCertificateAuthority:
              allowedKeyAlgorithms: ${schema.spec.allowedKeyAlgorithms}
          contact: "${schema.spec.contact}"
          certificateTags: ${schema.spec.certificateTags}
          tags: >-
            ${(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedAnnotations.orValue({}) : {})
              .merge(schema.spec.tags)
              .merge(schema.spec.syncedLabels)
              .merge(schema.spec.syncedAnnotations)
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedAnnotations.orValue({}) : {})
              .transformList(k, v, {"key": k, "value": v})
              .sortBy(x, x.key)}
