# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# aws-lambda-05: LambdaFunctionURLConfig RGD
# Composes: lambda.services.k8s.aws/FunctionURLConfig
# Naming exempt (KRO-236): no effectiveName, resourceName, predictedArn, namingStatus.
#   FunctionURLConfig has no user-supplied cloud resource name; the URL is generated by AWS.
# No spec.tags on ACK FunctionURLConfig — governance tags forwarded to K8s metadata labels only.
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: lambdafunctionurlconfig.aws.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: aws.kropath.run
    kind: LambdaFunctionURLConfig
    scope: Namespaced
    spec:
      configRef: string | default=general-policy
      deletionPolicy: string | default=retain

      tags: map[string]string | default={}
      syncedLabels: map[string]string | default={}
      syncedAnnotations: map[string]string | default={}

      # Local LambdaFunction CR name — resolved via externalRef label selector
      functionRef: string | default=""

      # Authentication type: AWS_IAM (sigv4) or NONE (public URL)
      authType: string | default=""

      # Alias qualifier — omit (or empty) for $LATEST
      qualifier: string | default=""

      # CORS configuration for browser-based callers
      cors:
        allowCredentials: boolean | default=false
        allowHeaders: "[]string | default=[]"
        allowMethods: "[]string | default=[]"
        allowOrigins: "[]string | default=[]"
        exposeHeaders: "[]string | default=[]"
        maxAge: integer | default=0

    status:
      functionUrl: >-
        ${urlCr.?status.?functionURL.orValue("")}
      conditions: >-
        ${urlCr.?status.?conditions.orValue([])}

    additionalPrinterColumns:
      - name: FunctionUrl
        type: string
        jsonPath: .status.functionUrl

  resources:
    # ── governance config lookup ──────────────────────────────────────────────
    - id: rsrcCfg
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: LambdaConfig
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: ${schema.?spec.?configRef.orValue("general-policy")}

    # ── parent function reference ─────────────────────────────────────────────
    # Sentinel keeps externalRef bound as [] when functionRef is empty.
    - id: lambdaFnCr
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: LambdaFunction
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: '${schema.spec.functionRef != "" ? schema.spec.functionRef : "kro-empty-fallback-sentinel"}'

    # ── ACK FunctionURLConfig child resource ──────────────────────────────────
    - id: urlCr
      template:
        apiVersion: lambda.services.k8s.aws/v1alpha1
        kind: FunctionURLConfig
        metadata:
          name: ${schema.metadata.name}
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && has(rsrcCfg[0].status.effectiveConfig.defaults.tags)) ? rsrcCfg[0].status.effectiveConfig.defaults.tags.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.tags.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && has(rsrcCfg[0].status.effectiveConfig.mandatory.tags)) ? rsrcCfg[0].status.effectiveConfig.mandatory.tags.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: LambdaFunctionURLConfig
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          authType: "${schema.spec.authType}"
          qualifier: "${schema.spec.qualifier}"
          # ACK cross-resource reference — resolves the target Lambda Function CR
          functionRef:
            from:
              name: "${schema.spec.functionRef}"
          cors:
            allowCredentials: ${schema.spec.cors.allowCredentials}
            allowHeaders: ${schema.spec.cors.allowHeaders}
            allowMethods: ${schema.spec.cors.allowMethods}
            allowOrigins: ${schema.spec.cors.allowOrigins}
            exposeHeaders: ${schema.spec.cors.exposeHeaders}
            maxAge: ${schema.spec.cors.maxAge}
