# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: s3bucket.aws.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: aws.kropath.run
    kind: S3Bucket
    scope: Namespaced
    spec:
      configRef: string | default=general-policy
      nameOverride: string | default=""
      deletionPolicy: string | default=retain
      region: string | default=""
      tags: map[string]string | default={}
      syncedLabels: map[string]string | default={}
      syncedAnnotations: map[string]string | default={}
      encryption:
        algorithm: string | default=""
        kmsKeyArn: string | default=""
        bucketKeyEnabled: boolean | default=true
      blockPublicAccess: boolean
      versioning: string | default=""
      enforceHttpsOnly: boolean
      objectOwnership: string | default=BucketOwnerEnforced
    status:
      resourceName: >-
        ${bucket.spec.name}
      namingStatus: >-
        ${bucket.spec.name.contains("{") ? "invalid-unresolved-tokens" : "valid"}
      predictedArn: >-
        ${"arn:aws:s3:::" + bucket.spec.name}
  resources:
    - id: rsrcCfg
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: S3Config
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: ${schema.?spec.?configRef.orValue("general-policy")}
    - id: bucket
      template:
        apiVersion: s3.services.k8s.aws/v1alpha1
        kind: Bucket
        metadata:
          name: ${schema.metadata.name}
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels.transformMapEntry(k, v, {"kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedLabels.transformMapEntry(k, v, {"kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels.transformMapEntry(k, v, {"kropath.run/" + k: v}) : {})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations.transformMapEntry(k, v, {"kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedAnnotations.transformMapEntry(k, v, {"kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations.transformMapEntry(k, v, {"kropath.run/" + k: v}) : {})}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: S3Bucket
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          name: >-
            ${(schema.spec.nameOverride != ""
              ? schema.spec.nameOverride
              : (rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.namingTemplate != ""
                  ? rsrcCfg[0].status.effectiveConfig.mandatory.namingTemplate
                  : (rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.defaults.namingTemplate != ""
                      ? rsrcCfg[0].status.effectiveConfig.defaults.namingTemplate
                      : schema.metadata.name))
                .replace("{namespace}", schema.metadata.namespace)
                .replace("{name}", schema.metadata.name)
                .replace("{account_id}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?accountId.orValue("") : "")
                .replace("{region}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?region.orValue("") : "")
                .replace("{configRef}", schema.spec.configRef)
            ).lowerAscii()}
          createBucketConfiguration:
            locationConstraint: >-
              ${schema.spec.region != "" && schema.spec.region != "us-east-1"
                ? schema.spec.region
                : ""}
          encryption:
            rules:
              - applyServerSideEncryptionByDefault:
                  sseAlgorithm: >-
                    ${rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.encryptionAlgorithm != ""
                      ? rsrcCfg[0].status.effectiveConfig.mandatory.encryptionAlgorithm
                      : (schema.spec.encryption.algorithm != ""
                          ? schema.spec.encryption.algorithm
                          : (rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.defaults.encryptionAlgorithm != ""
                              ? rsrcCfg[0].status.effectiveConfig.defaults.encryptionAlgorithm
                              : "aws:kms"))}
                  kmsMasterKeyID: >-
                    ${rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.kmsKeyArn != ""
                      ? rsrcCfg[0].status.effectiveConfig.mandatory.kmsKeyArn
                      : (schema.spec.encryption.kmsKeyArn != ""
                          ? schema.spec.encryption.kmsKeyArn
                          : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.kmsKeyArn : ""))}
                bucketKeyEnabled: ${schema.spec.encryption.bucketKeyEnabled}
          publicAccessBlock:
            blockPublicACLs: >-
              ${(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.blockPublicAccess)
                ? true
                : (schema.spec.?blockPublicAccess.orValue(false)
                    ? true
                    : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.blockPublicAccess : false))}
            blockPublicPolicy: >-
              ${(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.blockPublicAccess)
                ? true
                : (schema.spec.?blockPublicAccess.orValue(false)
                    ? true
                    : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.blockPublicAccess : false))}
            ignorePublicACLs: >-
              ${(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.blockPublicAccess)
                ? true
                : (schema.spec.?blockPublicAccess.orValue(false)
                    ? true
                    : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.blockPublicAccess : false))}
            restrictPublicBuckets: >-
              ${(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.blockPublicAccess)
                ? true
                : (schema.spec.?blockPublicAccess.orValue(false)
                    ? true
                    : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.blockPublicAccess : false))}
          versioning:
            status: >-
              ${rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.versioning != ""
                ? rsrcCfg[0].status.effectiveConfig.mandatory.versioning
                : (schema.spec.versioning != ""
                    ? schema.spec.versioning
                    : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.versioning : ""))}
          policy: >-
            ${((rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.enforceHttpsOnly)
              ? true
              : (schema.spec.?enforceHttpsOnly.orValue(false)
                  ? true
                  : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.enforceHttpsOnly : false)))
              ? "{\"Version\":\"2012-10-17\",\"Statement\":[{\"Sid\":\"DenyNonTLSAccess\",\"Effect\":\"Deny\",\"Principal\":\"*\",\"Action\":\"s3:*\",\"Resource\":[\"arn:aws:s3:::__BNAME__\",\"arn:aws:s3:::__BNAME__/*\"],\"Condition\":{\"Bool\":{\"aws:SecureTransport\":\"false\"}}}]}"
                .replace("__BNAME__", (schema.spec.nameOverride != ""
                  ? schema.spec.nameOverride
                  : (rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.namingTemplate != ""
                      ? rsrcCfg[0].status.effectiveConfig.mandatory.namingTemplate
                      : (rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.defaults.namingTemplate != ""
                          ? rsrcCfg[0].status.effectiveConfig.defaults.namingTemplate
                          : schema.metadata.name))
                    .replace("{namespace}", schema.metadata.namespace)
                    .replace("{name}", schema.metadata.name)
                    .replace("{account_id}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?accountId.orValue("") : "")
                    .replace("{region}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?region.orValue("") : "")
                    .replace("{configRef}", schema.spec.configRef)
                ).lowerAscii())
              : ""}
          tagging: >-
            ${(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.tags : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations : {})
              .merge(schema.spec.tags)
              .merge(schema.spec.syncedLabels)
              .merge(schema.spec.syncedAnnotations)
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.tags : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations : {})
              .transformList(k, v, {"key": k, "value": v})}
          objectOwnership: >-
            ${schema.spec.objectOwnership != "" ? schema.spec.objectOwnership : "BucketOwnerEnforced"}
          ownershipControls:
            rules:
              - objectOwnership: >-
                  ${schema.spec.objectOwnership != "" ? schema.spec.objectOwnership : "BucketOwnerEnforced"}
