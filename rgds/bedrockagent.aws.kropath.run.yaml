---
# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Spec: kropath-core/docs/specs/aws/aws-bedrock-03-bedrockagent.md
# CRD cache: kropath-core/docs/crd-cache/aws/bedrockagent-v1.3.1.md
#
# Governance cascade (BedrockConfig mandatory > spec > defaults):
#   foundationModel, guardrailIdentifier, guardrailVersion, idleSessionTTLInSeconds
#
# Mutual exclusivity: agentResourceRoleArn XOR agentResourceRoleRef (ConfigMap advisory).
# ACK field name: agentResourceRoleARN (uppercase ARN).
# Integer sentinel: 0 = "not set", falls through to next tier.
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: bedrockagent.aws.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: aws.kropath.run
    kind: BedrockAgent
    scope: Namespaced
    spec:
      configRef: string | default="general-policy"
      nameOverride: string | default=""
      deletionPolicy: string | default=retain
      foundationModel: string | default=""
      guardrailConfiguration: GuardrailConfiguration | default={}
      idleSessionTTLInSeconds: integer | default=0
      allowedModels: "[]string | default=[]"
      instruction: string | default=""
      agentResourceRoleArn: string | default=""
      agentResourceRoleRef: string | default=""
      customerEncryptionKeyArn: string | default=""
      tags: map[string]string | default={}
      syncedLabels: map[string]string | default={}
      syncedAnnotations: map[string]string | default={}
    types:
      GuardrailConfiguration:
        guardrailIdentifier: string | default=""
        guardrailVersion: string | default=""
    status:
      resourceName: >-
        ${naming.data.effectiveName}
      namingStatus: >-
        ${naming.data.effectiveName.contains("{") ? "invalid-unresolved-tokens" : "valid"}
      predictedArn: >-
        ${ackAgentRef.size() > 0 ? ackAgentRef[0].?status.?ackResourceMetadata.?arn.orValue("") : ""}
      agentId: >-
        ${ackAgentRef.size() > 0 ? ackAgentRef[0].?status.?agentID.orValue("") : ""}
      conditions: >-
        ${ackAgentRef.size() > 0 ? ackAgentRef[0].?status.?conditions.orValue([]) : []}
    additionalPrinterColumns:
      - name: ResourceName
        type: string
        jsonPath: .status.resourceName
      - name: NamingStatus
        type: string
        jsonPath: .status.namingStatus
      - name: PredictedArn
        type: string
        jsonPath: .status.predictedArn

  resources:
    # Config lookup.
    - id: rsrcCfg
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: BedrockConfig
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: ${schema.spec.?configRef.orValue("general-policy")}

    # Naming ConfigMap.
    - id: naming
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.metadata.name}-naming
          namespace: ${schema.metadata.namespace}
          labels:
            app.kubernetes.io/managed-by: kro
            app.kubernetes.io/instance: ${schema.metadata.name}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: BedrockAgent
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        data:
          effectiveName: >-
            ${((schema.spec.nameOverride != ""
              ? schema.spec.nameOverride
              : ((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && rsrcCfg[0].status.effectiveConfig.mandatory.?namingTemplate.orValue("") != "")
                  ? rsrcCfg[0].status.effectiveConfig.mandatory.namingTemplate
                  : ((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && rsrcCfg[0].status.effectiveConfig.defaults.?namingTemplate.orValue("") != "")
                      ? rsrcCfg[0].status.effectiveConfig.defaults.namingTemplate
                      : "{namespace}-{name}")))
              .split("{tag.")
              .transformList(i, part,
                i == 0 ? part
                : ((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.mandatory.tags
                      ? rsrcCfg[0].status.effectiveConfig.mandatory.tags[part.split("}")[0]]
                      : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.mandatory.?syncedLabels.orValue({})
                          ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels[part.split("}")[0]]
                          : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.mandatory.?syncedAnnotations.orValue({})
                              ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations[part.split("}")[0]]
                              : (part.split("}")[0] in schema.spec.tags
                                  ? schema.spec.tags[part.split("}")[0]]
                                  : (part.split("}")[0] in schema.spec.syncedLabels
                                      ? schema.spec.syncedLabels[part.split("}")[0]]
                                      : (part.split("}")[0] in schema.spec.syncedAnnotations
                                          ? schema.spec.syncedAnnotations[part.split("}")[0]]
                                          : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.defaults.tags
                                              ? rsrcCfg[0].status.effectiveConfig.defaults.tags[part.split("}")[0]]
                                              : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.defaults.?syncedLabels.orValue({})
                                                  ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels[part.split("}")[0]]
                                                  : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.defaults.?syncedAnnotations.orValue({})
                                                      ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations[part.split("}")[0]]
                                                      : "{tag." + part.split("}")[0] + "}")))))))))
                  + (size(part.split("}")) > 1
                      ? part.split("}").slice(1, size(part.split("}"))).join("}")
                      : "")))
              .join("")
              .replace("{namespace}", schema.metadata.namespace)
              .replace("{name}", schema.metadata.name)
              .replace("{account_id}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?accountId.orValue("") : "")
              .replace("{region}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?region.orValue("") : "")
              .replace("{configRef}", schema.spec.configRef))}

    # IAMRole lookup for agentResourceRoleRef cross-family resolution.
    # Looks up IAMRole by aws.kropath.run/resource-name label.
    # Uses kro-empty-fallback-sentinel when agentResourceRoleRef is "" to ensure empty list.
    - id: agentRoleRef
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: IAMRole
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: '${schema.spec.agentResourceRoleRef != "" ? schema.spec.agentResourceRoleRef : "kro-empty-fallback-sentinel"}'

    # ConfigMap advisory: fires when both agentResourceRoleArn AND agentResourceRoleRef are set.
    # x-kubernetes-validations are silently dropped by kro on re-apply; use this pattern instead.
    - id: mutualExclusionRoleError
      includeWhen:
        - '${schema.spec.agentResourceRoleArn != "" && schema.spec.agentResourceRoleRef != ""}'
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.metadata.name}-role-exclusion-error
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: BedrockAgent
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        data:
          error: "agentResourceRoleArn and agentResourceRoleRef are mutually exclusive — set exactly one"

    # ACK Agent resource. Governance fields cascade: mandatory > spec > defaults.
    - id: ackAgent
      template:
        apiVersion: bedrockagent.services.k8s.aws/v1alpha1
        kind: Agent
        metadata:
          name: ${naming.data.effectiveName}
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: BedrockAgent
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          agentName: ${naming.data.effectiveName}
          foundationModel: >-
            ${rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?foundationModel.orValue("") != ""
              ? rsrcCfg[0].status.effectiveConfig.mandatory.foundationModel
              : (schema.spec.foundationModel != ""
                  ? schema.spec.foundationModel
                  : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?foundationModel.orValue("") : ""))}
          guardrailConfiguration:
            guardrailIdentifier: >-
              ${rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?guardrailIdentifier.orValue("") != ""
                ? rsrcCfg[0].status.effectiveConfig.mandatory.guardrailIdentifier
                : (schema.spec.guardrailConfiguration.?guardrailIdentifier.orValue("") != ""
                    ? schema.spec.guardrailConfiguration.guardrailIdentifier
                    : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?guardrailIdentifier.orValue("") : ""))}
            guardrailVersion: >-
              ${rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?guardrailVersion.orValue("") != ""
                ? rsrcCfg[0].status.effectiveConfig.mandatory.guardrailVersion
                : (schema.spec.guardrailConfiguration.?guardrailVersion.orValue("") != ""
                    ? schema.spec.guardrailConfiguration.guardrailVersion
                    : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?guardrailVersion.orValue("") : ""))}
          idleSessionTTLInSeconds: >-
            ${rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?idleSessionTTLInSeconds.orValue(0) != 0
              ? rsrcCfg[0].status.effectiveConfig.mandatory.idleSessionTTLInSeconds
              : (schema.spec.idleSessionTTLInSeconds != 0
                  ? schema.spec.idleSessionTTLInSeconds
                  : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?idleSessionTTLInSeconds.orValue(0) : 0))}
          agentResourceRoleARN: >-
            ${schema.spec.agentResourceRoleArn != ""
              ? schema.spec.agentResourceRoleArn
              : (agentRoleRef.size() > 0 ? agentRoleRef[0].?status.?predictedArn.orValue("") : "")}
          instruction: ${schema.spec.?instruction.orValue("")}
          customerEncryptionKeyARN: ${schema.spec.?customerEncryptionKeyArn.orValue("")}
          tags: >-
            ${(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedAnnotations.orValue({}) : {})
              .merge(schema.spec.tags)
              .merge(schema.spec.syncedLabels)
              .merge(schema.spec.syncedAnnotations)
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedAnnotations.orValue({}) : {})
              .transformList(k, v, {"key": k, "value": v})
              .sortBy(x, x.key)}

    # Always-bound self-lookup for the Agent's own ACK child.
    - id: ackAgentRef
      externalRef:
        apiVersion: bedrockagent.services.k8s.aws/v1alpha1
        kind: Agent
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              app.kubernetes.io/instance: ${schema.metadata.name}
