# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: ec2subnet.aws.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: aws.kropath.run
    kind: EC2Subnet
    scope: Namespaced
    spec:
      configRef: string | default=general-policy
      deletionPolicy: string | default=retain
      tags: map[string]string | default={}
      syncedLabels: map[string]string | default={}
      syncedAnnotations: map[string]string | default={}
      # Required: VPC this subnet belongs to (AWS VPC ID).
      vpcId: string
      # Required: CIDR block for the subnet.
      cidrBlock: string
      # Required: availability zone (e.g. us-east-1a).
      availabilityZone: string
      # Governance: mandatory → instance → defaults.
      # true = restrict public IP assignment on launch (maps to mapPublicIPOnLaunch=false).
      # Bare boolean — no default to avoid tri-state collapse (see CLAUDE.md).
      restrictPublicIpOnLaunch: boolean
      # Optional IPv6 CIDR block.
      ipv6CidrBlock: string | default=""
      # Optional route table IDs to associate with this subnet.
      routeTableIds: "[]string | default=[]"
    status:
      subnetID: >-
        ${ackSubnet.?status.?subnetID.orValue("")}
      state: >-
        ${ackSubnet.?status.?state.orValue("")}
      availableIpAddressCount: >-
        ${ackSubnet.?status.?availableIPAddressCount.orValue(0)}
      conditions: >-
        ${ackSubnet.?status.?conditions.orValue([])}
    additionalPrinterColumns:
      - name: SubnetID
        type: string
        jsonPath: .status.subnetID
      - name: State
        type: string
        jsonPath: .status.state
  resources:
    # Single governance config lookup — controller pre-merges all cascade layers (ADR-010 D-1).
    - id: rsrcCfg
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: EC2Config
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: ${schema.?spec.?configRef.orValue("general-policy")}

    # ACK Subnet CR — primary cloud resource.
    - id: ackSubnet
      template:
        apiVersion: ec2.services.k8s.aws/v1alpha1
        kind: Subnet
        metadata:
          name: ${schema.metadata.name}
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: EC2Subnet
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          vpcID: ${schema.spec.vpcId}
          cidrBlock: ${schema.spec.cidrBlock}
          availabilityZone: ${schema.spec.availabilityZone}
          # restrictPublicIpOnLaunch governance: mandatory → instance → defaults.
          # Inverted: restrictPublicIp=true → mapPublicIPOnLaunch=false.
          mapPublicIPOnLaunch: >-
            ${!(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?restrictPublicIpOnLaunch.orValue(false)
                ? true
                : ((has(schema.spec.restrictPublicIpOnLaunch) && schema.spec.restrictPublicIpOnLaunch)
                    ? true
                    : (rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.defaults.?restrictPublicIpOnLaunch.orValue(false))))}
          ipv6CIDRBlock: ${schema.spec.ipv6CidrBlock}
          routeTables: ${schema.spec.routeTableIds}
          # Cloud resource tags — ACK EC2 Subnet spec.tags is array<{key, value}> (not map).
          tags: >-
            ${(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedAnnotations.orValue({}) : {})
              .merge(schema.spec.tags)
              .merge(schema.spec.syncedLabels)
              .merge(schema.spec.syncedAnnotations)
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedAnnotations.orValue({}) : {})
              .transformList(k, v, {"key": k, "value": v})}
