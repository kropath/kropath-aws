# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: awsiamrole.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: kropath.run
    kind: AWSIAMRole
    scope: Namespaced
    spec:
      configRef: string | default="general-policy"
      type: string | required=true
      description: string | default=""
      nameOverride: string | default=""
      deletionPolicy: string | default="retain" enum="retain,delete"
      tags: "map[string]string | default={}"
      syncedLabels: "map[string]string | default={}"
      syncedAnnotations: "map[string]string | default={}"
      policies: "[]RolePolicy | default=[]"
      inlinePolicies: "[]InlinePolicy | default=[]"
      trustPolicyRef: string | default=""
      permissionsBoundaryArn: string | default=""
      oidcProviderArn: string | default=""
      serviceAccountNamespace: string | default=""
      serviceAccountName: string | default=""
      clusterName: string | default=""
      servicePrincipal: string | default=""
      trustPolicyJSON: string | default=""
      addSsmPolicy: boolean | default=true
      addEcsExecutionPolicy: boolean | default=true
      addLambdaBasicPolicy: boolean | default=true
      createInstanceProfile: boolean | default=true
      maxSessionDuration: integer | default=0 minimum=0 maximum=43200
    types:
      RolePolicy:
        arn: string | default=""
        ref: string | default=""
        inline:
          name: string | default=""
          documentJSON: string | default=""
          documentRef: string | default=""
      InlinePolicy:
        name: string | required=true
        documentRef: string | required=true
    status:
      resourceName: >-
        ${schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name)}
      namingStatus: >-
        ${!(schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name).contains("{")) ? "valid" : "invalid-unresolved-tokens"}
      predictedArn: >-
        ${"arn:aws:iam::" + (rsrcCfg.status.effectiveConfig.aws.accountId ?? "") + ":role/" + (schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name))}
      conditions: >-
        ${role.status.conditions}
    additionalPrinterColumns:
      - name: Type
        type: string
        jsonPath: .spec.type
      - name: ResourceName
        type: string
        jsonPath: .status.resourceName
      - name: PredictedArn
        type: string
        jsonPath: .status.predictedArn
      - name: Naming
        type: string
        jsonPath: .status.namingStatus
  resources:
    - id: rsrcCfg
      externalRef:
        apiVersion: kropath.run/v1alpha1
        kind: AWSIAMConfig
        metadata:
          name: ${schema.spec.configRef}
          namespace: ${schema.metadata.namespace}
    - id: trustDoc
      includeWhen:
        - "${schema.spec.trustPolicyRef != \"\"}"
      externalRef:
        apiVersion: kropath.run/v1alpha1
        kind: AWSPolicyDocument
        metadata:
          name: ${schema.spec.trustPolicyRef}
          namespace: ${schema.metadata.namespace}
    - id: firstPolicyRef
      includeWhen:
        - "${schema.spec.policies.size() > 0 && schema.spec.policies[0].ref != \"\"}"
      externalRef:
        apiVersion: kropath.run/v1alpha1
        kind: AWSIAMPolicy
        metadata:
          name: ${schema.spec.policies[0].ref}
          namespace: ${schema.metadata.namespace}
    - id: firstInlineDoc
      includeWhen:
        - "${schema.spec.inlinePolicies.size() > 0 && schema.spec.inlinePolicies[0].documentRef != \"\"}"
      externalRef:
        apiVersion: kropath.run/v1alpha1
        kind: AWSPolicyDocument
        metadata:
          name: ${schema.spec.inlinePolicies[0].documentRef}
          namespace: ${schema.metadata.namespace}
    - id: role
      includeWhen:
        - >-
          ${!(schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name).contains("{"))}
      template:
        apiVersion: iam.services.k8s.aws/v1alpha1
        kind: Role
        metadata:
          name: >-
            ${schema.metadata.name}
          namespace: >-
            ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name} + (rsrcCfg.status.effectiveConfig.mandatory.syncedLabels + schema.spec.syncedLabels + rsrcCfg.status.effectiveConfig.defaults.syncedLabels).transformMapEntry(k, v, {"kropath.run/" + k: v})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy} + (rsrcCfg.status.effectiveConfig.mandatory.syncedAnnotations + schema.spec.syncedAnnotations + rsrcCfg.status.effectiveConfig.defaults.syncedAnnotations).transformMapEntry(k, v, {"kropath.run/" + k: v})}
          ownerReferences:
            - apiVersion: kropath.run/v1alpha1
              kind: AWSIAMRole
              name: >-
                ${schema.metadata.name}
              uid: >-
                ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          name: >-
            ${schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name)}
          description: >-
            ${schema.spec.description}
          assumeRolePolicyDocument: >-
            ${schema.spec.trustPolicyRef != "" ? trustDoc.spec.documentJSON : (schema.spec.type == "ec2" ? '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"ec2.amazonaws.com"},"Action":"sts:AssumeRole"}]}' : (schema.spec.type == "ecs-task" ? '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"ecs-tasks.amazonaws.com"},"Action":"sts:AssumeRole"}]}' : (schema.spec.type == "lambda" ? '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"lambda.amazonaws.com"},"Action":"sts:AssumeRole"}]}' : (schema.spec.type == "eks-irsa" ? '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Federated":"' + schema.spec.oidcProviderArn + '"},"Action":"sts:AssumeRoleWithWebIdentity","Condition":{"StringEquals":{"sts.amazonaws.com:sub":"system:serviceaccount:' + schema.spec.serviceAccountNamespace + ':' + schema.spec.serviceAccountName + '"}}}]}' : (schema.spec.type == "eks-pod-identity" ? '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"pods.eks.amazonaws.com"},"Action":"sts:AssumeRole"}]}' : (schema.spec.type == "aws-service" ? '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"' + schema.spec.servicePrincipal + '"},"Action":"sts:AssumeRole"}]}' : schema.spec.trustPolicyJSON))))))}
          permissionsBoundary: >-
            ${rsrcCfg.status.effectiveConfig.mandatory.permissionsBoundaryArn != "" ? rsrcCfg.status.effectiveConfig.mandatory.permissionsBoundaryArn : (schema.spec.permissionsBoundaryArn == "none" ? "" : (schema.spec.permissionsBoundaryArn != "" ? schema.spec.permissionsBoundaryArn : rsrcCfg.status.effectiveConfig.defaults.permissionsBoundaryArn)) != "" ? rsrcCfg.status.effectiveConfig.mandatory.permissionsBoundaryArn != "" ? rsrcCfg.status.effectiveConfig.mandatory.permissionsBoundaryArn : (schema.spec.permissionsBoundaryArn == "none" ? "" : (schema.spec.permissionsBoundaryArn != "" ? schema.spec.permissionsBoundaryArn : rsrcCfg.status.effectiveConfig.defaults.permissionsBoundaryArn)) : omit()}
          maxSessionDuration: >-
            ${(rsrcCfg.status.effectiveConfig.mandatory.maxSessionDurationSeconds > 0 && (schema.spec.maxSessionDuration != 0 ? schema.spec.maxSessionDuration : rsrcCfg.status.effectiveConfig.defaults.maxSessionDurationSeconds) > rsrcCfg.status.effectiveConfig.mandatory.maxSessionDurationSeconds ? rsrcCfg.status.effectiveConfig.mandatory.maxSessionDurationSeconds : ((schema.spec.maxSessionDuration != 0 ? schema.spec.maxSessionDuration : rsrcCfg.status.effectiveConfig.defaults.maxSessionDurationSeconds) > 0 ? (schema.spec.maxSessionDuration != 0 ? schema.spec.maxSessionDuration : rsrcCfg.status.effectiveConfig.defaults.maxSessionDurationSeconds) : 3600))}
          policies: >-
            ${( (schema.spec.type == "ec2" && schema.spec.addSsmPolicy ? ["arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore"] : []) + (schema.spec.type == "ecs-task" && schema.spec.addEcsExecutionPolicy ? ["arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy"] : []) + (schema.spec.type == "lambda" && schema.spec.addLambdaBasicPolicy ? ["arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole"] : []) + schema.spec.policies.filter(p, p.arn != "" || p.ref != "").transformList(i, p, p.arn != "" ? p.arn : firstPolicyRef.status.arn) )}
          inlinePolicies: >-
            ${( schema.spec.inlinePolicies + schema.spec.policies.filter(p, p.inline.name != "").transformList(i, p, p.inline) ).transformMapEntry(i, p, {p.name: (p.documentRef != "" ? firstInlineDoc.spec.documentJSON : p.documentJSON)})}
          tags: >-
            ${(rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags) + (rsrcCfg.status.effectiveConfig.mandatory.syncedLabels + schema.spec.syncedLabels + rsrcCfg.status.effectiveConfig.defaults.syncedLabels).transformMapEntry(k, v, {"kropath.run/" + k: v}) + (rsrcCfg.status.effectiveConfig.mandatory.syncedAnnotations + schema.spec.syncedAnnotations + rsrcCfg.status.effectiveConfig.defaults.syncedAnnotations).transformMapEntry(k, v, {"kropath.run/" + k: v}).transformList(i, key, {key: key, value: (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags) + (rsrcCfg.status.effectiveConfig.mandatory.syncedLabels + schema.spec.syncedLabels + rsrcCfg.status.effectiveConfig.defaults.syncedLabels).transformMapEntry(k, v, {"kropath.run/" + k: v}) + (rsrcCfg.status.effectiveConfig.mandatory.syncedAnnotations + schema.spec.syncedAnnotations + rsrcCfg.status.effectiveConfig.defaults.syncedAnnotations).transformMapEntry(k, v, {"kropath.run/" + k: v}).?[key].orValue("")})}

    - id: instanceProfile
      includeWhen:
        - >-
          ${!(schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name).contains("{")) && schema.spec.type == "ec2" && schema.spec.createInstanceProfile}
      template:
        apiVersion: iam.services.k8s.aws/v1alpha1
        kind: InstanceProfile
        metadata:
          name: >-
            ${schema.metadata.name}
          namespace: >-
            ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name} + (rsrcCfg.status.effectiveConfig.mandatory.syncedLabels + schema.spec.syncedLabels + rsrcCfg.status.effectiveConfig.defaults.syncedLabels).transformMapEntry(k, v, {"kropath.run/" + k: v})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy} + (rsrcCfg.status.effectiveConfig.mandatory.syncedAnnotations + schema.spec.syncedAnnotations + rsrcCfg.status.effectiveConfig.defaults.syncedAnnotations).transformMapEntry(k, v, {"kropath.run/" + k: v})}
          ownerReferences:
            - apiVersion: kropath.run/v1alpha1
              kind: AWSIAMRole
              name: >-
                ${schema.metadata.name}
              uid: >-
                ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          name: >-
            ${schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name)}
          role: >-
            ${schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name)}

    - id: podIdentityAssociation
      includeWhen:
        - >-
          ${!(schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name).contains("{")) && schema.spec.type == "eks-pod-identity"}
      template:
        apiVersion: eks.services.k8s.aws/v1alpha1
        kind: PodIdentityAssociation
        metadata:
          name: >-
            ${schema.metadata.name}
          namespace: >-
            ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name} + (rsrcCfg.status.effectiveConfig.mandatory.syncedLabels + schema.spec.syncedLabels + rsrcCfg.status.effectiveConfig.defaults.syncedLabels).transformMapEntry(k, v, {"kropath.run/" + k: v})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy} + (rsrcCfg.status.effectiveConfig.mandatory.syncedAnnotations + schema.spec.syncedAnnotations + rsrcCfg.status.effectiveConfig.defaults.syncedAnnotations).transformMapEntry(k, v, {"kropath.run/" + k: v})}
          ownerReferences:
            - apiVersion: kropath.run/v1alpha1
              kind: AWSIAMRole
              name: >-
                ${schema.metadata.name}
              uid: >-
                ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          clusterName: >-
            ${schema.spec.clusterName}
          namespace: >-
            ${schema.spec.serviceAccountNamespace}
          serviceAccount: >-
            ${schema.spec.serviceAccountName}
          roleARN: >-
            ${"arn:aws:iam::" + (rsrcCfg.status.effectiveConfig.aws.accountId ?? "") + ":role/" + (schema.spec.nameOverride != "" ? schema.spec.nameOverride : ((rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate) != "" ? (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != "" ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate : rsrcCfg.status.effectiveConfig.defaults.namingTemplate).split("{tag.") .transformList(i, part, i == 0 ? part : (((rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") != "" ? (rsrcCfg.status.effectiveConfig.mandatory.tags + schema.spec.tags + rsrcCfg.status.effectiveConfig.defaults.tags).?[part.split("}")[0]].orValue("") : "{tag." + part.split("}")[0] + "}") + (size(part.split("}")) > 1 ? part.split("}").slice(1, size(part.split("}"))).join("}") : ""))) .join("") .replace("{name}", schema.metadata.name) .replace("{namespace}", schema.metadata.namespace) .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.accountId ?? "") .replace("{region}", rsrcCfg.status.effectiveConfig.aws.region ?? "") .replace("{configRef}", schema.spec.configRef) : schema.metadata.name))}
