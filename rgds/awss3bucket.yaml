# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: awss3bucket.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: kropath.run
    kind: AWSS3Bucket
    scope: Namespaced
    spec:
      configRef: string | default=general-policy
      nameOverride: string | default=""
      deletionPolicy: string | default=retain
      region: string | default=""
      tags: map[string]string | default={}
      syncedLabels: map[string]string | default={}
      syncedAnnotations: map[string]string | default={}
      encryption:
        algorithm: string | default=""
        kmsKeyArn: string | default=""
        bucketKeyEnabled: boolean | default=true
      blockPublicAccess: boolean
      versioning: string | default=""
      enforceHttpsOnly: boolean
      objectOwnership: string | default=BucketOwnerEnforced
    status:
      resourceName: >-
        ${bucket.spec.name}
      namingStatus: >-
        ${bucket.spec.name.contains("{") ? "invalid-unresolved-tokens" : "valid"}
      predictedArn: >-
        ${"arn:aws:s3:::" + bucket.spec.name}
  resources:
    - id: rsrcCfg
      externalRef:
        apiVersion: kropath.run/v1alpha1
        kind: AWSS3Config
        metadata:
          name: ${schema.?spec.?configRef.orValue("general-policy")}
          namespace: ${schema.metadata.namespace}
    - id: bucket
      template:
        apiVersion: s3.services.k8s.aws/v1alpha1
        kind: Bucket
        metadata:
          name: ${schema.metadata.name}
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}
              .merge(rsrcCfg.status.effectiveConfig.defaults.syncedLabels.transformMapEntry(k, v, {"kropath.run/" + k: v}))
              .merge(schema.spec.syncedLabels.transformMapEntry(k, v, {"kropath.run/" + k: v}))
              .merge(rsrcCfg.status.effectiveConfig.mandatory.syncedLabels.transformMapEntry(k, v, {"kropath.run/" + k: v}))}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy}
              .merge(rsrcCfg.status.effectiveConfig.defaults.syncedAnnotations.transformMapEntry(k, v, {"kropath.run/" + k: v}))
              .merge(schema.spec.syncedAnnotations.transformMapEntry(k, v, {"kropath.run/" + k: v}))
              .merge(rsrcCfg.status.effectiveConfig.mandatory.syncedAnnotations.transformMapEntry(k, v, {"kropath.run/" + k: v}))}
          ownerReferences:
            - apiVersion: kropath.run/v1alpha1
              kind: AWSS3Bucket
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          name: >-
            ${(schema.spec.nameOverride != ""
              ? schema.spec.nameOverride
              : (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != ""
                  ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate
                  : (rsrcCfg.status.effectiveConfig.defaults.namingTemplate != ""
                      ? rsrcCfg.status.effectiveConfig.defaults.namingTemplate
                      : schema.metadata.name))
                .replace("{namespace}", schema.metadata.namespace)
                .replace("{name}", schema.metadata.name)
                .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.?accountId.orValue(""))
                .replace("{region}", rsrcCfg.status.effectiveConfig.aws.?region.orValue(""))
                .replace("{configRef}", schema.spec.configRef)
            ).lowerAscii()}
          createBucketConfiguration:
            locationConstraint: >-
              ${schema.spec.region != "" && schema.spec.region != "us-east-1"
                ? schema.spec.region
                : ""}
          encryption:
            rules:
              - applyServerSideEncryptionByDefault:
                  sseAlgorithm: >-
                    ${rsrcCfg.status.effectiveConfig.mandatory.encryptionAlgorithm != ""
                      ? rsrcCfg.status.effectiveConfig.mandatory.encryptionAlgorithm
                      : (schema.spec.encryption.algorithm != ""
                          ? schema.spec.encryption.algorithm
                          : (rsrcCfg.status.effectiveConfig.defaults.encryptionAlgorithm != ""
                              ? rsrcCfg.status.effectiveConfig.defaults.encryptionAlgorithm
                              : "aws:kms"))}
                  kmsMasterKeyID: >-
                    ${rsrcCfg.status.effectiveConfig.mandatory.kmsKeyArn != ""
                      ? rsrcCfg.status.effectiveConfig.mandatory.kmsKeyArn
                      : (schema.spec.encryption.kmsKeyArn != ""
                          ? schema.spec.encryption.kmsKeyArn
                          : rsrcCfg.status.effectiveConfig.defaults.kmsKeyArn)}
                bucketKeyEnabled: ${schema.spec.encryption.bucketKeyEnabled}
          publicAccessBlock:
            blockPublicACLs: >-
              ${rsrcCfg.status.effectiveConfig.mandatory.blockPublicAccess
                ? true
                : (schema.spec.?blockPublicAccess.orValue(false)
                    ? true
                    : rsrcCfg.status.effectiveConfig.defaults.blockPublicAccess)}
            blockPublicPolicy: >-
              ${rsrcCfg.status.effectiveConfig.mandatory.blockPublicAccess
                ? true
                : (schema.spec.?blockPublicAccess.orValue(false)
                    ? true
                    : rsrcCfg.status.effectiveConfig.defaults.blockPublicAccess)}
            ignorePublicACLs: >-
              ${rsrcCfg.status.effectiveConfig.mandatory.blockPublicAccess
                ? true
                : (schema.spec.?blockPublicAccess.orValue(false)
                    ? true
                    : rsrcCfg.status.effectiveConfig.defaults.blockPublicAccess)}
            restrictPublicBuckets: >-
              ${rsrcCfg.status.effectiveConfig.mandatory.blockPublicAccess
                ? true
                : (schema.spec.?blockPublicAccess.orValue(false)
                    ? true
                    : rsrcCfg.status.effectiveConfig.defaults.blockPublicAccess)}
          versioning:
            status: >-
              ${rsrcCfg.status.effectiveConfig.mandatory.versioning != ""
                ? rsrcCfg.status.effectiveConfig.mandatory.versioning
                : (schema.spec.versioning != ""
                    ? schema.spec.versioning
                    : rsrcCfg.status.effectiveConfig.defaults.versioning)}
          policy: >-
            ${(rsrcCfg.status.effectiveConfig.mandatory.enforceHttpsOnly
              ? true
              : (schema.spec.?enforceHttpsOnly.orValue(false)
                  ? true
                  : rsrcCfg.status.effectiveConfig.defaults.enforceHttpsOnly))
              ? "{\"Version\":\"2012-10-17\",\"Statement\":[{\"Sid\":\"DenyNonTLSAccess\",\"Effect\":\"Deny\",\"Principal\":\"*\",\"Action\":\"s3:*\",\"Resource\":[\"arn:aws:s3:::__BNAME__\",\"arn:aws:s3:::__BNAME__/*\"],\"Condition\":{\"Bool\":{\"aws:SecureTransport\":\"false\"}}}]}"
                .replace("__BNAME__", (schema.spec.nameOverride != ""
                  ? schema.spec.nameOverride
                  : (rsrcCfg.status.effectiveConfig.mandatory.namingTemplate != ""
                      ? rsrcCfg.status.effectiveConfig.mandatory.namingTemplate
                      : (rsrcCfg.status.effectiveConfig.defaults.namingTemplate != ""
                          ? rsrcCfg.status.effectiveConfig.defaults.namingTemplate
                          : schema.metadata.name))
                    .replace("{namespace}", schema.metadata.namespace)
                    .replace("{name}", schema.metadata.name)
                    .replace("{account_id}", rsrcCfg.status.effectiveConfig.aws.?accountId.orValue(""))
                    .replace("{region}", rsrcCfg.status.effectiveConfig.aws.?region.orValue(""))
                    .replace("{configRef}", schema.spec.configRef)
                ).lowerAscii())
              : ""}
          tagging: >-
            ${rsrcCfg.status.effectiveConfig.defaults.tags
              .merge(rsrcCfg.status.effectiveConfig.defaults.syncedLabels)
              .merge(rsrcCfg.status.effectiveConfig.defaults.syncedAnnotations)
              .merge(schema.spec.tags)
              .merge(schema.spec.syncedLabels)
              .merge(schema.spec.syncedAnnotations)
              .merge(rsrcCfg.status.effectiveConfig.mandatory.tags)
              .merge(rsrcCfg.status.effectiveConfig.mandatory.syncedLabels)
              .merge(rsrcCfg.status.effectiveConfig.mandatory.syncedAnnotations)
              .transformList(k, v, {"key": k, "value": v})}
          objectOwnership: >-
            ${schema.spec.objectOwnership != "" ? schema.spec.objectOwnership : "BucketOwnerEnforced"}
          ownershipControls:
            rules:
              - objectOwnership: >-
                  ${schema.spec.objectOwnership != "" ? schema.spec.objectOwnership : "BucketOwnerEnforced"}
