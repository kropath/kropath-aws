---
# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Spec: kropath-core/docs/specs/aws/aws-bedrock-06-bedrockagentruntime.md
# CRD cache: kropath-core/docs/crd-cache/aws/bedrockagentcorecontrol-v1.14.0.md
#
# No governance cascade beyond common fields (naming, tags, syncedLabels, syncedAnnotations).
# ACK field: roleARN (uppercase ARN). ACK field: agentRuntimeName (immutable after creation).
# Mutual exclusivity: containerConfiguration XOR codeConfiguration (ConfigMap advisory).
# predictedArn sourced from ACK status (AWS-assigned runtime ID, not effectiveName).
# Optional complex fields omitted from ACK child when not set (kro omits empty-value fields).
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: bedrockagentruntime.aws.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: aws.kropath.run
    kind: BedrockAgentRuntime
    scope: Namespaced
    spec:
      configRef: string | default="general-policy"
      nameOverride: string | default=""
      deletionPolicy: string | default=retain
      tags: "map[string]string | default={}"
      syncedLabels: "map[string]string | default={}"
      syncedAnnotations: "map[string]string | default={}"
      # Required
      agentRuntimeArtifact: BedrockAgentRuntimeArtifact
      networkConfiguration: BedrockNetworkConfiguration
      roleArn: string
      # Optional — declare with | default={} or | default="" so field is optional in CRD.
      # Bare sub-fields (no | default=) prevent kro from materializing zeros for unset branches.
      description: string | default=""
      environmentVariables: "map[string]string | default={}"
      lifecycleConfiguration: "BedrockLifecycleConfiguration | default={}"
      authorizerConfiguration: "BedrockRuntimeAuthorizerConfiguration | default={}"
      filesystemConfigurations: "[]BedrockFilesystemConfiguration | default=[]"
      protocolConfiguration: "BedrockRuntimeProtocolConfiguration | default={}"
      requestHeaderConfiguration: "BedrockRequestHeaderConfiguration | default={}"
    types:
      BedrockAgentRuntimeArtifact:
        containerConfiguration: BedrockContainerConfiguration
        codeConfiguration: BedrockCodeConfiguration
      BedrockContainerConfiguration:
        containerURI: string
      BedrockCodeConfiguration:
        code: BedrockCodeSource
        entryPoint: "[]string | default=[]"
        runtime: string
      BedrockCodeSource:
        s3: BedrockS3CodeSource
      BedrockS3CodeSource:
        bucket: string
        prefix: string
        versionID: string
      BedrockNetworkConfiguration:
        networkMode: string
        networkModeConfig: BedrockNetworkModeConfig
      BedrockNetworkModeConfig:
        securityGroups: "[]string | default=[]"
        subnets: "[]string | default=[]"
      BedrockLifecycleConfiguration:
        idleRuntimeSessionTimeout: integer
        maxLifetime: integer
      BedrockRuntimeAuthorizerConfiguration:
        customJWTAuthorizer: BedrockCustomJWTAuthorizerConfig
      BedrockCustomJWTAuthorizerConfig:
        allowedAudience: "[]string | default=[]"
        allowedClients: "[]string | default=[]"
        allowedScopes: "[]string | default=[]"
        discoveryURL: string
        customClaims: "[]BedrockCustomClaim | default=[]"
      BedrockCustomClaim:
        inboundTokenClaimName: string
        inboundTokenClaimValueType: string
        authorizingClaimMatchValue: BedrockAuthorizingClaimMatchValue
      BedrockAuthorizingClaimMatchValue:
        claimMatchOperator: string
        claimMatchValue: BedrockClaimMatchValue
      BedrockClaimMatchValue:
        matchValueString: string
        matchValueStringList: "[]string | default=[]"
      BedrockFilesystemConfiguration:
        sessionStorage: BedrockSessionStorageConfig
      BedrockSessionStorageConfig:
        mountPath: string
      BedrockRuntimeProtocolConfiguration:
        serverProtocol: string
      BedrockRequestHeaderConfiguration:
        requestHeaderAllowlist: "[]string | default=[]"
    status:
      resourceName: >-
        ${naming.data.effectiveName}
      namingStatus: >-
        ${naming.data.effectiveName.contains("{") ? "invalid-unresolved-tokens" : "valid"}
      predictedArn: >-
        ${ackRuntimeRef.size() > 0 ? ackRuntimeRef[0].?status.?ackResourceMetadata.?arn.orValue("") : ""}
      conditions: >-
        ${ackRuntimeRef.size() > 0 ? ackRuntimeRef[0].?status.?conditions.orValue([]) : []}
    additionalPrinterColumns:
      - name: ResourceName
        type: string
        jsonPath: .status.resourceName
      - name: NamingStatus
        type: string
        jsonPath: .status.namingStatus

  resources:
    # Config lookup.
    - id: rsrcCfg
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: BedrockConfig
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: ${schema.spec.?configRef.orValue("general-policy")}

    # Naming ConfigMap.
    - id: naming
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.metadata.name}-naming
          namespace: ${schema.metadata.namespace}
          labels:
            app.kubernetes.io/managed-by: kro
            app.kubernetes.io/instance: ${schema.metadata.name}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: BedrockAgentRuntime
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        data:
          effectiveName: >-
            ${((schema.spec.nameOverride != ""
              ? schema.spec.nameOverride
              : ((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && rsrcCfg[0].status.effectiveConfig.mandatory.?namingTemplate.orValue("") != "")
                  ? rsrcCfg[0].status.effectiveConfig.mandatory.namingTemplate
                  : ((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && rsrcCfg[0].status.effectiveConfig.defaults.?namingTemplate.orValue("") != "")
                      ? rsrcCfg[0].status.effectiveConfig.defaults.namingTemplate
                      : "{namespace}-{name}")))
              .split("{tag.")
              .transformList(i, part,
                i == 0 ? part
                : ((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.mandatory.tags
                      ? rsrcCfg[0].status.effectiveConfig.mandatory.tags[part.split("}")[0]]
                      : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.mandatory.?syncedLabels.orValue({})
                          ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels[part.split("}")[0]]
                          : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.mandatory.?syncedAnnotations.orValue({})
                              ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations[part.split("}")[0]]
                              : (part.split("}")[0] in schema.spec.tags
                                  ? schema.spec.tags[part.split("}")[0]]
                                  : (part.split("}")[0] in schema.spec.syncedLabels
                                      ? schema.spec.syncedLabels[part.split("}")[0]]
                                      : (part.split("}")[0] in schema.spec.syncedAnnotations
                                          ? schema.spec.syncedAnnotations[part.split("}")[0]]
                                          : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.defaults.tags
                                              ? rsrcCfg[0].status.effectiveConfig.defaults.tags[part.split("}")[0]]
                                              : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.defaults.?syncedLabels.orValue({})
                                                  ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels[part.split("}")[0]]
                                                  : (rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && part.split("}")[0] in rsrcCfg[0].status.effectiveConfig.defaults.?syncedAnnotations.orValue({})
                                                      ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations[part.split("}")[0]]
                                                      : "{tag." + part.split("}")[0] + "}")))))))))
                  + (size(part.split("}")) > 1
                      ? part.split("}").slice(1, size(part.split("}"))).join("}")
                      : "")))
              .join("")
              .replace("{namespace}", schema.metadata.namespace)
              .replace("{name}", schema.metadata.name)
              .replace("{account_id}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?accountId.orValue("") : "")
              .replace("{region}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?region.orValue("") : "")
              .replace("{configRef}", schema.spec.configRef))}

    # ConfigMap advisory: fires when both containerConfiguration AND codeConfiguration are set.
    - id: mutualExclusionArtifactError
      includeWhen:
        - '${schema.spec.agentRuntimeArtifact.?containerConfiguration.?containerURI.orValue("") != "" && schema.spec.agentRuntimeArtifact.?codeConfiguration.?runtime.orValue("") != ""}'
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.metadata.name}-artifact-exclusion-error
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: BedrockAgentRuntime
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        data:
          error: "spec.agentRuntimeArtifact.containerConfiguration and spec.agentRuntimeArtifact.codeConfiguration are mutually exclusive; set only one."

    # ACK AgentRuntime resource.
    - id: ackRuntime
      template:
        apiVersion: bedrockagentcorecontrol.services.k8s.aws/v1alpha1
        kind: AgentRuntime
        metadata:
          name: ${naming.data.effectiveName}
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: BedrockAgentRuntime
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          agentRuntimeName: ${naming.data.effectiveName}
          agentRuntimeArtifact: ${schema.spec.agentRuntimeArtifact}
          networkConfiguration: ${schema.spec.networkConfiguration}
          roleARN: ${schema.spec.roleArn}
          description: ${schema.spec.?description.orValue("")}
          environmentVariables: ${schema.spec.environmentVariables}
          lifecycleConfiguration: ${schema.spec.lifecycleConfiguration}
          authorizerConfiguration: ${schema.spec.authorizerConfiguration}
          filesystemConfigurations: ${schema.spec.filesystemConfigurations}
          protocolConfiguration: ${schema.spec.protocolConfiguration}
          requestHeaderConfiguration: ${schema.spec.requestHeaderConfiguration}
          tags: >-
            ${(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedAnnotations.orValue({}) : {})
              .merge(schema.spec.tags)
              .merge(schema.spec.syncedLabels)
              .merge(schema.spec.syncedAnnotations)
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedAnnotations.orValue({}) : {})}

    # Always-bound self-lookup for ACK AgentRuntime child status.
    - id: ackRuntimeRef
      externalRef:
        apiVersion: bedrockagentcorecontrol.services.k8s.aws/v1alpha1
        kind: AgentRuntime
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              app.kubernetes.io/instance: ${schema.metadata.name}
