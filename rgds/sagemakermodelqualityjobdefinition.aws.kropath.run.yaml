# Copyright 2024 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: sagemakermodelqualityjobdefinition.aws.kropath.run
spec:
  schema:
    apiVersion: v1alpha1
    group: aws.kropath.run
    kind: SageMakerModelQualityJobDefinition
    scope: Namespaced
    spec:
      configRef: string | default=general-policy
      nameOverride: string | default=""
      deletionPolicy: string | default=retain

      tags: map[string]string | default={}
      syncedLabels: map[string]string | default={}
      syncedAnnotations: map[string]string | default={}

      # Required
      roleArn: string
      modelQualityAppSpecification: object
      modelQualityJobInput: object
      modelQualityJobOutputConfig: object

      # jobResources.clusterConfig sub-fields (explicit for governance)
      clusterConfigInstanceType: string | default=""
      clusterConfigInstanceCount: integer | default=1
      clusterConfigVolumeSizeInGB: integer | default=0

      # Governed
      kmsKeyId: string | default=""
      enableNetworkIsolation: boolean | default=false

      # Optional pass-through
      vpcConfig: object | default={}
      stoppingCondition: object | default={}
      modelQualityBaselineConfig: object | default={}

    status:
      resourceName: >-
        ${naming.data.effectiveName}
      namingStatus: >-
        ${naming.data.effectiveName.contains("{") ? "invalid-unresolved-tokens" : "valid"}
      predictedArn: >-
        ${"arn:aws:sagemaker:" + (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?region.orValue("") : "") + ":" + (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?accountId.orValue("") : "") + ":model-quality-job-definition/" + naming.data.effectiveName}
      conditions: >-
        ${ackMqjdRef.size() > 0 ? ackMqjdRef[0].?status.?conditions.orValue([]) : []}
    additionalPrinterColumns:
      - name: ResourceName
        type: string
        jsonPath: .status.resourceName
      - name: NamingStatus
        type: string
        jsonPath: .status.namingStatus

  resources:
    - id: rsrcCfg
      externalRef:
        apiVersion: aws.kropath.run/v1alpha1
        kind: SageMakerConfig
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              aws.kropath.run/resource-name: ${schema.?spec.?configRef.orValue("general-policy")}

    - id: naming
      template:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: ${schema.metadata.name}-naming
          namespace: ${schema.metadata.namespace}
          labels:
            app.kubernetes.io/managed-by: kro
            app.kubernetes.io/instance: ${schema.metadata.name}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: SageMakerModelQualityJobDefinition
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        data:
          effectiveName: >-
            ${((schema.spec.nameOverride != ""
              ? schema.spec.nameOverride
              : ((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && rsrcCfg[0].status.effectiveConfig.mandatory.?namingTemplate.orValue("") != "")
                  ? rsrcCfg[0].status.effectiveConfig.mandatory.namingTemplate
                  : ((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && rsrcCfg[0].status.effectiveConfig.defaults.?namingTemplate.orValue("") != "")
                      ? rsrcCfg[0].status.effectiveConfig.defaults.namingTemplate
                      : "{namespace}-{name}")))
              .replace("{namespace}", schema.metadata.namespace)
              .replace("{name}", schema.metadata.name)
              .replace("{account_id}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?accountId.orValue("") : "")
              .replace("{region}", rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.aws.?region.orValue("") : "")
              .replace("{configRef}", schema.spec.configRef))}

    - id: ackMqjdRef
      externalRef:
        apiVersion: sagemaker.services.k8s.aws/v1alpha1
        kind: ModelQualityJobDefinition
        metadata:
          namespace: ${schema.metadata.namespace}
          selector:
            matchLabels:
              app.kubernetes.io/instance: ${schema.metadata.name}

    - id: ackModelQualityJobDefinition
      includeWhen:
        - ${!naming.data.effectiveName.contains("{")}
      template:
        apiVersion: sagemaker.services.k8s.aws/v1alpha1
        kind: ModelQualityJobDefinition
        metadata:
          name: ${schema.metadata.name}
          namespace: ${schema.metadata.namespace}
          labels: >-
            ${{"app.kubernetes.io/managed-by": "kro", "app.kubernetes.io/instance": schema.metadata.name}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedLabels.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          annotations: >-
            ${{"services.k8s.aws/deletion-policy": schema.spec.deletionPolicy}
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.defaults) && has(rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.defaults.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})
              .merge(schema.spec.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}))
              .merge((rsrcCfg.size() > 0 && has(rsrcCfg[0].status.effectiveConfig.mandatory) && has(rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations)) ? rsrcCfg[0].status.effectiveConfig.mandatory.syncedAnnotations.transformMapEntry(k, v, {"aws.kropath.run/" + k: v}) : {})}
          ownerReferences:
            - apiVersion: aws.kropath.run/v1alpha1
              kind: SageMakerModelQualityJobDefinition
              name: ${schema.metadata.name}
              uid: ${schema.metadata.uid}
              controller: true
              blockOwnerDeletion: true
        spec:
          jobDefinitionName: ${naming.data.effectiveName}
          roleARN: ${schema.spec.roleArn}
          modelQualityAppSpecification: ${schema.spec.modelQualityAppSpecification}
          modelQualityJobInput: ${schema.spec.modelQualityJobInput}
          modelQualityJobOutputConfig: ${schema.spec.modelQualityJobOutputConfig}
          modelQualityBaselineConfig: ${schema.spec.modelQualityBaselineConfig}
          stoppingCondition: ${schema.spec.stoppingCondition}
          jobResources:
            clusterConfig:
              instanceType: >-
                ${(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?instanceType.orValue("") != "")
                  ? rsrcCfg[0].status.effectiveConfig.mandatory.instanceType
                  : (schema.spec.clusterConfigInstanceType != ""
                      ? schema.spec.clusterConfigInstanceType
                      : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?instanceType.orValue("") : ""))}
              instanceCount: ${schema.spec.clusterConfigInstanceCount}
              volumeSizeInGB: >-
                ${(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?volumeSizeInGB.orValue(0) > 0)
                  ? rsrcCfg[0].status.effectiveConfig.mandatory.volumeSizeInGB
                  : (schema.spec.clusterConfigVolumeSizeInGB > 0
                      ? schema.spec.clusterConfigVolumeSizeInGB
                      : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?volumeSizeInGB.orValue(0) : 0))}
              volumeKMSKeyID: >-
                ${(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?kmsKeyId.orValue("") != "")
                  ? rsrcCfg[0].status.effectiveConfig.mandatory.kmsKeyId
                  : (schema.spec.kmsKeyId != ""
                      ? schema.spec.kmsKeyId
                      : (rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?kmsKeyId.orValue("") : ""))}
          networkConfig:
            enableNetworkIsolation: >-
              ${(rsrcCfg.size() > 0 && rsrcCfg[0].status.effectiveConfig.mandatory.?enableNetworkIsolation.orValue(false))
                ? true
                : schema.spec.enableNetworkIsolation}
            vpcConfig: ${schema.spec.vpcConfig}
          tags: >-
            ${(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.defaults.?syncedAnnotations.orValue({}) : {})
              .merge(schema.spec.tags)
              .merge(schema.spec.syncedLabels)
              .merge(schema.spec.syncedAnnotations)
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?tags.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedLabels.orValue({}) : {})
              .merge(rsrcCfg.size() > 0 ? rsrcCfg[0].status.effectiveConfig.mandatory.?syncedAnnotations.orValue({}) : {})
              .transformList(k, v, {"key": k, "value": v})}
