Rendered {{ len .Values.families }} <Family>Config object(s) plus the
Namespace "{{ .Values.namespace }}" into this output. Commit the rendered manifests to your
Argo/Flux repo — this chart applies nothing itself.

──────────────────────────────────────────────────────────────────────────────────────────────
ACTION STILL REQUIRED — ack-role-account-map (not rendered by this chart; see README.md
"Why ack-role-account-map is not rendered here" for the full feasibility finding)
──────────────────────────────────────────────────────────────────────────────────────────────

ACK resolves the account it actually creates resources in from the IAM role ARN bound to
{{ .Values.accountId }} in the `ack-role-account-map` ConfigMap — not from the
owner-account-id annotation this chart did render. If that ConfigMap has no entry for
{{ .Values.accountId }} (or the entry's role ARN lives in a different account), ACK and
kropath will disagree about where this namespace's resources land, and nothing will error
(ADR-015 §5.8.4 precondition 3/4).

Ask whoever owns the ACK install (cluster/platform operators — this ConfigMap lives in the
ACK system namespace, a different ownership boundary than this tenant namespace) to confirm
an entry exists:

  "{{ .Values.accountId }}": "arn:aws:iam::{{ .Values.accountId }}:role/<ACK-assumed-role>"

The role ARN's account segment MUST equal {{ .Values.accountId }} — a role ARN in a different
account is the single most likely placement-mismatch vector (ADR-015 §5.8.4 precondition 4).
