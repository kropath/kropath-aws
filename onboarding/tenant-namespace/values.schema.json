{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "tenant-namespace-onboarding values",
  "type": "object",
  "required": ["namespace", "namespaceRole", "families"],
  "properties": {
    "namespace": {
      "type": "string",
      "minLength": 1,
      "description": "Namespace being onboarded."
    },
    "namespaceRole": {
      "type": "string",
      "enum": ["global", "local"],
      "description": "ADR-019 D-5 role. 'local' is a resource namespace — requires globalConfigNamespace, accountId, and region, and renders all three placement annotations. 'global' is a governance-only namespace — renders none of those annotations and does not need those three fields."
    },
    "globalConfigNamespace": {
      "type": "string",
      "description": "Namespace holding the global-tier KropathConfig/<Family>Config baseline. Required (and must be non-empty) when namespaceRole is 'local'; unused when 'global'."
    },
    "accountId": {
      "type": "string",
      "description": "12-digit AWS account ID (ADR-015 §5.8.2 rule 1). Required (and pattern-checked) when namespaceRole is 'local'; unused when 'global'."
    },
    "region": {
      "type": "string",
      "description": "AWS region, e.g. ap-southeast-2. Required (and must be non-empty) when namespaceRole is 'local'; unused when 'global'."
    },
    "configRef": {
      "type": "string",
      "minLength": 1,
      "default": "general-policy"
    },
    "families": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "description": "Family slugs to onboard — see family-kind-map.yaml for accepted values."
    }
  },
  "if": {
    "properties": { "namespaceRole": { "const": "local" } }
  },
  "then": {
    "required": ["globalConfigNamespace", "accountId", "region"],
    "properties": {
      "globalConfigNamespace": { "minLength": 1 },
      "accountId": { "pattern": "^[0-9]{12}$" },
      "region": { "minLength": 1 }
    }
  }
}
