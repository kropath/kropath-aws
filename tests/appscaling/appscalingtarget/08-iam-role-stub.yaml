apiVersion: aws.kropath.run/v1alpha1
kind: IAMRole
metadata:
  name: appscaling-role-stub
  namespace: appscalingtarget
  labels:
    aws.kropath.run/resource-name: appscaling-role-stub
spec:
  configRef: general-policy
  type: ec2
  deletionPolicy: retain
