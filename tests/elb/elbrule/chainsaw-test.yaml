# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# aws-elb-05 ELBRule Chainsaw tests — one scenario per acceptance criterion (15 ACs).
# Namespace: elbrule (fixed; Rule has no cloud resource name — ARN-identified only).
# accountId: 123456789012 | region: us-east-1
# effectiveConfig is injected via kubectl patch --subresource=status to simulate
# kropath-controller (which is not running in the local test cluster).
# Tags are lists ([]Tag{key,value}) — jq script steps used for order-independent assertions.
# spec.nameOverride is a documented no-op: ACK Rule has no name field.
# No status.resourceName or status.namingStatus — naming does not apply to Rule.
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: elbrule
spec:
  namespace: elbrule
  steps:
    # ──────────────────────────────────────────────────────────────────────────
    # Purge: delete all resources this test may have created in a prior run.
    # ──────────────────────────────────────────────────────────────────────────
    - name: purge-stale-leftovers
      try:
        - apply:
            file: 00-elbrule-namespace.yaml
        - script:
            content: |
              set -e
              NS=elbrule
              # Delete ELBRule CRs
              for name in path-fwd redir fixed host-hdr multi-cond ord-fwd \
                          tags-mrg slbl-dual sanno std-meta del-del \
                          del-ret cfg-custom name-noop prio-100 \
                          path-pat-cfg http-hdr-cfg http-mth-cfg qstr-cfg srcip-cfg wt-fwd; do
                kubectl delete elbrule "${name}" -n "${NS}" --ignore-not-found=true
              done
              # Delete ELBConfig CRs
              for name in general-policy tags-policy synced-labels-policy \
                          synced-anno-policy custom-config; do
                kubectl delete elbconfig "${name}" -n "${NS}" --ignore-not-found=true
              done

    # ──────────────────────────────────────────────────────────────────────────
    # Seed: all governance ELBConfigs with effectiveConfig patched.
    # ──────────────────────────────────────────────────────────────────────────
    - name: seed-configs
      try:
        - script:
            content: |
              set -e
              NS=elbrule

              kubectl apply -f - <<'EOF'
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBConfig
              metadata:
                name: general-policy
                namespace: elbrule
                labels:
                  aws.kropath.run/resource-name: general-policy
              spec:
                mandatory:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags: {}
                  syncedLabels: {}
                  syncedAnnotations: {}
                defaults:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags: {}
                  syncedLabels: {}
                  syncedAnnotations: {}
              EOF
              kubectl apply -f - <<'EOF'
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBConfig
              metadata:
                name: tags-policy
                namespace: elbrule
                labels:
                  aws.kropath.run/resource-name: tags-policy
              spec:
                mandatory:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags:
                    cost-centre: platform
                  syncedLabels: {}
                  syncedAnnotations: {}
                defaults:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags:
                    defaults-tag: from-defaults
                  syncedLabels: {}
                  syncedAnnotations: {}
              EOF
              kubectl apply -f - <<'EOF'
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBConfig
              metadata:
                name: synced-labels-policy
                namespace: elbrule
                labels:
                  aws.kropath.run/resource-name: synced-labels-policy
              spec:
                mandatory:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags: {}
                  syncedLabels:
                    environment: production
                  syncedAnnotations: {}
                defaults:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags: {}
                  syncedLabels: {}
                  syncedAnnotations: {}
              EOF
              kubectl apply -f - <<'EOF'
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBConfig
              metadata:
                name: synced-anno-policy
                namespace: elbrule
                labels:
                  aws.kropath.run/resource-name: synced-anno-policy
              spec:
                mandatory:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags: {}
                  syncedLabels: {}
                  syncedAnnotations:
                    team: infra
                defaults:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags: {}
                  syncedLabels: {}
                  syncedAnnotations: {}
              EOF
              kubectl apply -f - <<'EOF'
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBConfig
              metadata:
                name: custom-config
                namespace: elbrule
                labels:
                  aws.kropath.run/resource-name: custom-config
              spec:
                mandatory:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags:
                    custom-env: staging
                  syncedLabels: {}
                  syncedAnnotations: {}
                defaults:
                  deletionProtection: false
                  accessLogsEnabled: false
                  accessLogsS3Bucket: ""
                  crossZoneEnabled: false
                  internalOnly: false
                  idleTimeoutSeconds: 0
                  sslPolicy: ""
                  namingTemplate: ""
                  tags: {}
                  syncedLabels: {}
                  syncedAnnotations: {}
              EOF

              # Patch effectiveConfig on all configs (simulate kropath-controller)
              BASE='{"status":{"effectiveConfig":{"mandatory":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{},"syncedLabels":{},"syncedAnnotations":{}},"defaults":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{},"syncedLabels":{},"syncedAnnotations":{}},"aws":{"accountId":"123456789012","region":"us-east-1"}}}}'
              kubectl patch elbconfig general-policy -n elbrule --subresource=status --type=merge -p "$BASE"
              kubectl patch elbconfig tags-policy -n elbrule --subresource=status --type=merge -p '{"status":{"effectiveConfig":{"mandatory":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{"cost-centre":"platform"},"syncedLabels":{},"syncedAnnotations":{}},"defaults":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{"defaults-tag":"from-defaults"},"syncedLabels":{},"syncedAnnotations":{}},"aws":{"accountId":"123456789012","region":"us-east-1"}}}}'
              kubectl patch elbconfig synced-labels-policy -n elbrule --subresource=status --type=merge -p '{"status":{"effectiveConfig":{"mandatory":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{},"syncedLabels":{"environment":"production"},"syncedAnnotations":{}},"defaults":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{},"syncedLabels":{},"syncedAnnotations":{}},"aws":{"accountId":"123456789012","region":"us-east-1"}}}}'
              kubectl patch elbconfig synced-anno-policy -n elbrule --subresource=status --type=merge -p '{"status":{"effectiveConfig":{"mandatory":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{},"syncedLabels":{},"syncedAnnotations":{"team":"infra"}},"defaults":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{},"syncedLabels":{},"syncedAnnotations":{}},"aws":{"accountId":"123456789012","region":"us-east-1"}}}}'
              kubectl patch elbconfig custom-config -n elbrule --subresource=status --type=merge -p '{"status":{"effectiveConfig":{"mandatory":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{"custom-env":"staging"},"syncedLabels":{},"syncedAnnotations":{}},"defaults":{"deletionProtection":false,"accessLogsEnabled":false,"accessLogsS3Bucket":"","crossZoneEnabled":false,"internalOnly":false,"idleTimeoutSeconds":0,"sslPolicy":"","namingTemplate":"","tags":{},"syncedLabels":{},"syncedAnnotations":{}},"aws":{"accountId":"123456789012","region":"us-east-1"}}}}'

    # ──────────────────────────────────────────────────────────────────────────
    # AC-1: path-pattern condition + forward action.
    # Verifies: listenerRef passthrough, priority, conditions field/values,
    #           actions targetGroupRef.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac1-forward-action
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: path-fwd
              spec:
                listenerRef: test-lb
                priority: 10
                conditions:
                  - field: path-pattern
                    values:
                      - /api/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: path-fwd
              spec:
                listenerRef:
                  from:
                    name: test-lb
                priority: 10
                conditions:
                  - field: path-pattern
                    values:
                      - /api/*
                actions:
                  - type: forward
                    targetGroupRef:
                      from:
                        name: test-tg

    # ──────────────────────────────────────────────────────────────────────────
    # AC-2: redirect action — type=redirect with redirectConfig fields.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac2-redirect-action
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: redir
              spec:
                listenerRef: test-lb
                priority: 20
                conditions:
                  - field: host-header
                    values:
                      - old.example.com
                actions:
                  - type: redirect
                    order: 1
                    redirectConfig:
                      protocol: HTTPS
                      statusCode: HTTP_301
                      port: "443"
                      host: new.example.com
                      path: "/#{path}"
                      query: "#{query}"
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: redir
              spec:
                actions:
                  - type: redirect
                    order: 1
                    redirectConfig:
                      protocol: HTTPS
                      statusCode: HTTP_301
                      port: "443"

    # ──────────────────────────────────────────────────────────────────────────
    # AC-3: fixed-response action — type=fixed-response with fixedResponseConfig.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac3-fixed-response-action
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: fixed
              spec:
                listenerRef: test-lb
                priority: 30
                conditions:
                  - field: path-pattern
                    values:
                      - /health
                actions:
                  - type: fixed-response
                    fixedResponseConfig:
                      statusCode: "200"
                      contentType: text/plain
                      messageBody: OK
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: fixed
              spec:
                actions:
                  - type: fixed-response
                    fixedResponseConfig:
                      statusCode: "200"
                      contentType: text/plain
                      messageBody: OK

    # ──────────────────────────────────────────────────────────────────────────
    # AC-4: host-header condition type — field=host-header, values passthrough.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac4-host-header-condition
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: host-hdr
              spec:
                listenerRef: test-lb
                priority: 40
                conditions:
                  - field: host-header
                    values:
                      - api.example.com
                      - www.example.com
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: host-hdr
              spec:
                conditions:
                  - field: host-header
                    values:
                      - api.example.com
                      - www.example.com

    # ──────────────────────────────────────────────────────────────────────────
    # AC-5: multiple conditions — path-pattern AND host-header in one rule.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac5-multiple-conditions
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: multi-cond
              spec:
                listenerRef: test-lb
                priority: 50
                conditions:
                  - field: path-pattern
                    values:
                      - /admin/*
                  - field: host-header
                    values:
                      - admin.example.com
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: multi-cond
              spec:
                conditions:
                  - field: path-pattern
                    values:
                      - /admin/*
                  - field: host-header
                    values:
                      - admin.example.com

    # ──────────────────────────────────────────────────────────────────────────
    # AC-6: forward action with explicit order field — verifies order passthrough.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac6-action-with-order
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: ord-fwd
              spec:
                listenerRef: test-lb
                priority: 60
                conditions:
                  - field: path-pattern
                    values:
                      - /weighted/*
                actions:
                  - type: forward
                    order: 5
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: ord-fwd
              spec:
                actions:
                  - type: forward
                    order: 5
                    targetGroupRef:
                      from:
                        name: test-tg

    # ──────────────────────────────────────────────────────────────────────────
    # AC-7: mandatory + defaults + spec tags all merge; mandatory wins on conflicts.
    # Config tags-policy: mandatory.tags={cost-centre:platform}, defaults.tags={defaults-tag:from-defaults}
    # Instance: spec.tags={env:prod, cost-centre:instance-value}
    # Expected: cost-centre=platform (mandatory wins), env=prod (spec), defaults-tag=from-defaults (defaults)
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac7-tags-three-tier-merge
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: tags-mrg
              spec:
                configRef: tags-policy
                listenerRef: test-lb
                priority: 70
                conditions:
                  - field: path-pattern
                    values:
                      - /tags/*
                tags:
                  env: prod
                  cost-centre: instance-value
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - script:
            content: |
              # Poll until kro has reconciled the child rules — a one-shot
              # read races kro's async reconcile (child not yet created).
              for __poll_wait_child in $(seq 1 60); do
                kubectl get rules.elbv2.services.k8s.aws tags-mrg -n elbrule >/dev/null 2>&1 && break
                sleep 1
              done
              TAGS=$(kubectl get rules.elbv2.services.k8s.aws tags-mrg -n elbrule \
                -o json | jq -c '.spec.tags')
              echo "$TAGS" | jq -e 'any(.key == "env" and .value == "prod")' \
                >/dev/null || { echo "FAIL: missing tag env=prod; tags=$TAGS"; exit 1; }
              echo "$TAGS" | jq -e 'any(.key == "defaults-tag" and .value == "from-defaults")' \
                >/dev/null || { echo "FAIL: missing tag defaults-tag=from-defaults; tags=$TAGS"; exit 1; }
              echo "$TAGS" | jq -e 'any(.key == "cost-centre" and .value == "platform")' \
                >/dev/null || { echo "FAIL: mandatory cost-centre=platform did not win; tags=$TAGS"; exit 1; }
              echo "PASS: all three tag tiers merged; mandatory wins on conflict"

    # ──────────────────────────────────────────────────────────────────────────
    # AC-8: syncedLabels dual-write — K8s metadata.labels with aws.kropath.run/ prefix
    #         AND cloud tags (plain key, no prefix).
    # Config synced-labels-policy: mandatory.syncedLabels={environment:production}
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac8-synced-labels-dual-write
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: slbl-dual
              spec:
                configRef: synced-labels-policy
                listenerRef: test-lb
                priority: 80
                conditions:
                  - field: path-pattern
                    values:
                      - /sync-labels/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: slbl-dual
                labels:
                  aws.kropath.run/environment: production
              spec: {}
        - script:
            content: |
              # Poll until kro has reconciled the child rules — a one-shot
              # read races kro's async reconcile (child not yet created).
              for __poll_wait_child in $(seq 1 60); do
                kubectl get rules.elbv2.services.k8s.aws slbl-dual -n elbrule >/dev/null 2>&1 && break
                sleep 1
              done
              TAGS=$(kubectl get rules.elbv2.services.k8s.aws slbl-dual -n elbrule \
                -o json | jq -c '.spec.tags')
              echo "$TAGS" | jq -e 'any(.key == "environment" and .value == "production")' \
                >/dev/null || { echo "FAIL: syncedLabel environment=production not in cloud tags; tags=$TAGS"; exit 1; }
              echo "PASS: syncedLabel in both K8s metadata.labels (prefixed) and cloud tags (plain)"

    # ──────────────────────────────────────────────────────────────────────────
    # AC-9: syncedAnnotations written to ACK Rule metadata.annotations
    #         with aws.kropath.run/ prefix.
    # Config synced-anno-policy: mandatory.syncedAnnotations={team:infra}
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac9-synced-annotations-metadata
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: sanno
              spec:
                configRef: synced-anno-policy
                listenerRef: test-lb
                priority: 90
                conditions:
                  - field: path-pattern
                    values:
                      - /anno/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: sanno
                annotations:
                  aws.kropath.run/team: infra
              spec: {}

    # ──────────────────────────────────────────────────────────────────────────
    # AC-10: Standard kropath metadata present on ACK Rule CR.
    # Verifies: managed-by and instance labels; deletion-policy annotation; ownerReference.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac10-standard-kropath-metadata
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: std-meta
              spec:
                listenerRef: test-lb
                priority: 100
                conditions:
                  - field: path-pattern
                    values:
                      - /meta/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: std-meta
                labels:
                  app.kubernetes.io/managed-by: kro
                  app.kubernetes.io/instance: std-meta
                annotations:
                  services.k8s.aws/deletion-policy: retain
              spec: {}
        - script:
            content: |
              # Poll until kro has reconciled the child rules — a one-shot
              # read races kro's async reconcile (child not yet created).
              for __poll_wait_child in $(seq 1 60); do
                kubectl get rules.elbv2.services.k8s.aws std-meta -n elbrule >/dev/null 2>&1 && break
                sleep 1
              done
              OWNER=$(kubectl get rules.elbv2.services.k8s.aws std-meta -n elbrule \
                -o json | jq -r '.metadata.ownerReferences[0].kind')
              [ "$OWNER" = "ELBRule" ] \
                || { echo "FAIL: ownerReference.kind should be ELBRule; got=$OWNER"; exit 1; }
              CTRL=$(kubectl get rules.elbv2.services.k8s.aws std-meta -n elbrule \
                -o json | jq -r '.metadata.ownerReferences[0].controller')
              [ "$CTRL" = "true" ] \
                || { echo "FAIL: ownerReference.controller should be true; got=$CTRL"; exit 1; }
              echo "PASS: ownerReference points to ELBRule with controller=true"

    # ──────────────────────────────────────────────────────────────────────────
    # AC-11: spec.deletionPolicy=delete → annotation services.k8s.aws/deletion-policy: delete.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac11-deletion-policy-delete
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: del-del
              spec:
                listenerRef: test-lb
                priority: 110
                deletionPolicy: delete
                conditions:
                  - field: path-pattern
                    values:
                      - /del/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: del-del
                annotations:
                  services.k8s.aws/deletion-policy: delete
              spec: {}

    # ──────────────────────────────────────────────────────────────────────────
    # AC-12: spec.deletionPolicy=retain (explicit) → annotation retain.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac12-deletion-policy-retain
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: del-ret
              spec:
                listenerRef: test-lb
                priority: 120
                deletionPolicy: retain
                conditions:
                  - field: path-pattern
                    values:
                      - /ret/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: del-ret
                annotations:
                  services.k8s.aws/deletion-policy: retain
              spec: {}

    # ──────────────────────────────────────────────────────────────────────────
    # AC-13: configRef selects non-default ELBConfig.
    # Config custom-config: mandatory.tags={custom-env:staging}
    # Verifies the custom ELBConfig is picked up via matchLabels selector.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac13-configref-custom
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: cfg-custom
              spec:
                configRef: custom-config
                listenerRef: test-lb
                priority: 130
                conditions:
                  - field: path-pattern
                    values:
                      - /custom/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - script:
            content: |
              # Poll until kro has reconciled the child rules — a one-shot
              # read races kro's async reconcile (child not yet created).
              for __poll_wait_child in $(seq 1 60); do
                kubectl get rules.elbv2.services.k8s.aws cfg-custom -n elbrule >/dev/null 2>&1 && break
                sleep 1
              done
              TAGS=$(kubectl get rules.elbv2.services.k8s.aws cfg-custom -n elbrule \
                -o json | jq -c '.spec.tags')
              echo "$TAGS" | jq -e 'any(.key == "custom-env" and .value == "staging")' \
                >/dev/null || { echo "FAIL: mandatory custom-env=staging not in tags; tags=$TAGS"; exit 1; }
              echo "PASS: custom-config mandatory tag picked up via configRef"

    # ──────────────────────────────────────────────────────────────────────────
    # AC-14: spec.nameOverride is a no-op for ELBRule.
    # ACK Rule has no name field — it is identified by its ARN.
    # metadata.name of the child Rule must equal the CR name, not nameOverride.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac14-name-override-noop
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: name-noop
              spec:
                listenerRef: test-lb
                priority: 140
                nameOverride: my-custom-rule
                conditions:
                  - field: path-pattern
                    values:
                      - /noop/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: name-noop
              spec: {}
        - script:
            content: |
              # Poll until kro has reconciled the child rules — a one-shot
              # read races kro's async reconcile (child not yet created).
              for __poll_wait_child in $(seq 1 60); do
                kubectl get rules.elbv2.services.k8s.aws name-noop -n elbrule >/dev/null 2>&1 && break
                sleep 1
              done
              NAME=$(kubectl get rules.elbv2.services.k8s.aws name-noop -n elbrule \
                -o json | jq -r '.metadata.name')
              [ "$NAME" = "name-noop" ] \
                || { echo "FAIL: Rule metadata.name should be 'name-noop'; got=$NAME"; exit 1; }
              HAS_SPEC_NAME=$(kubectl get rules.elbv2.services.k8s.aws name-noop -n elbrule \
                -o json | jq '.spec | has("name")')
              [ "$HAS_SPEC_NAME" = "false" ] \
                || { echo "FAIL: ACK Rule spec should not have a name field; got has(.spec.name)=$HAS_SPEC_NAME"; exit 1; }
              echo "PASS: nameOverride is a no-op; ACK Rule has no spec.name field"

    # ──────────────────────────────────────────────────────────────────────────
    # AC-15: priority integer passthrough — different priority value (100).
    # Verifies the priority field round-trips correctly as an integer.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac15-priority-passthrough
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: prio-100
              spec:
                listenerRef: test-lb
                priority: 999
                conditions:
                  - field: path-pattern
                    values:
                      - /catch-all/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: prio-100
              spec:
                priority: 999

    # ──────────────────────────────────────────────────────────────────────────
    # AC-16: pathPatternConfig sub-type — field=path-pattern using pathPatternConfig.values.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac16-path-pattern-config
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: path-pat-cfg
              spec:
                listenerRef: test-lb
                priority: 160
                conditions:
                  - field: path-pattern
                    pathPatternConfig:
                      values:
                        - /api/v2/*
                        - /api/v3/*
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: path-pat-cfg
              spec:
                conditions:
                  - field: path-pattern
                    pathPatternConfig:
                      values:
                        - /api/v2/*
                        - /api/v3/*

    # ──────────────────────────────────────────────────────────────────────────
    # AC-17: httpHeaderConfig sub-type — field=http-header with httpHeaderConfig.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac17-http-header-config
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: http-hdr-cfg
              spec:
                listenerRef: test-lb
                priority: 170
                conditions:
                  - field: http-header
                    httpHeaderConfig:
                      httpHeaderName: X-Custom-Header
                      values:
                        - value-a
                        - value-b
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: http-hdr-cfg
              spec:
                conditions:
                  - field: http-header
                    httpHeaderConfig:
                      httpHeaderName: X-Custom-Header
                      values:
                        - value-a
                        - value-b

    # ──────────────────────────────────────────────────────────────────────────
    # AC-18: httpRequestMethodConfig sub-type — field=http-request-method.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac18-http-method-config
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: http-mth-cfg
              spec:
                listenerRef: test-lb
                priority: 180
                conditions:
                  - field: http-request-method
                    httpRequestMethodConfig:
                      values:
                        - GET
                        - POST
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: http-mth-cfg
              spec:
                conditions:
                  - field: http-request-method
                    httpRequestMethodConfig:
                      values:
                        - GET
                        - POST

    # ──────────────────────────────────────────────────────────────────────────
    # AC-19: queryStringConfig sub-type — field=query-string with key/value pairs.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac19-query-string-config
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: qstr-cfg
              spec:
                listenerRef: test-lb
                priority: 190
                conditions:
                  - field: query-string
                    queryStringPairs:
                      - key: version
                        value: "2"
                      - key: debug
                        value: "true"
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: qstr-cfg
              spec:
                conditions:
                  - field: query-string
                    queryStringConfig:
                      values:
                        - key: version
                          value: "2"
                        - key: debug
                          value: "true"

    # ──────────────────────────────────────────────────────────────────────────
    # AC-20: sourceIPConfig sub-type — field=source-ip with CIDR values.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac20-source-ip-config
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: srcip-cfg
              spec:
                listenerRef: test-lb
                priority: 200
                conditions:
                  - field: source-ip
                    sourceIPConfig:
                      values:
                        - 10.0.0.0/8
                        - 192.168.0.0/16
                actions:
                  - type: forward
                    targetGroupRef: test-tg
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: srcip-cfg
              spec:
                conditions:
                  - field: source-ip
                    sourceIPConfig:
                      values:
                        - 10.0.0.0/8
                        - 192.168.0.0/16

    # ──────────────────────────────────────────────────────────────────────────
    # AC-21: weighted-forward action — forwardConfig with multiple target groups
    #         and weights; CEL maps to ACK actions[].forwardConfig.targetGroups[].targetGroupRef.
    # ──────────────────────────────────────────────────────────────────────────
    - name: ac21-weighted-forward
      try:
        - apply:
            resource:
              apiVersion: aws.kropath.run/v1alpha1
              kind: ELBRule
              metadata:
                name: wt-fwd
              spec:
                listenerRef: test-lb
                priority: 210
                conditions:
                  - field: path-pattern
                    values:
                      - /canary/*
                actions:
                  - type: forward
                    weightedTargetGroups:
                      - targetGroupRef: tg-stable
                        weight: 90
                      - targetGroupRef: tg-canary
                        weight: 10
                    forwardConfig:
                      targetGroupStickinessConfig:
                        enabled: false
                        durationSeconds: 0
        - assert:
            resource:
              apiVersion: elbv2.services.k8s.aws/v1alpha1
              kind: Rule
              metadata:
                name: wt-fwd
              spec:
                actions:
                  - type: forward
                    forwardConfig:
                      targetGroups:
                        - targetGroupRef:
                            from:
                              name: tg-stable
                          weight: 90
                        - targetGroupRef:
                            from:
                              name: tg-canary
                          weight: 10
