apiVersion: aws.kropath.run/v1alpha1
kind: MWAAConfig
metadata:
  name: dual-tier-environmentclass-test-03
  namespace: mwaaconfig
spec:
  mandatory:
    environmentClass: "mw1.small"
  defaults:
    environmentClass: "mw1.medium"
