---
# Minimal CRD fixture for acm.services.k8s.aws/AcmeEndpoint.
# Source: kropath-core/docs/crd-cache/aws/acm-controller-v1.8.1.md
# Used by kro to resolve schema when compiling ACMEEndpoint RGD.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: acmeendpoints.acm.services.k8s.aws
spec:
  group: acm.services.k8s.aws
  names:
    kind: AcmeEndpoint
    listKind: AcmeEndpointList
    plural: acmeendpoints
    singular: acmeendpoint
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AcmeEndpoint is the Schema for the AcmeEndpoints API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: AcmeEndpointSpec defines the desired state of AcmeEndpoint
            properties:
              authorizationBehavior:
                type: string
              certificateAuthority:
                description: Configuration for the certificate authority used by the ACME endpoint
                properties:
                  publicCertificateAuthority:
                    properties:
                      allowedKeyAlgorithms:
                        description: List of allowed key algorithms for certificates issued via this endpoint
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              certificateTags:
                description: Tags applied to certificates issued via this endpoint (immutable)
                items:
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
              contact:
                description: Contact requirement for ACME order processing (REQUIRED or NOT_REQUIRED)
                type: string
              tags:
                items:
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
            required:
            - authorizationBehavior
            - certificateAuthority
            type: object
          status:
            description: AcmeEndpointStatus defines the observed state of AcmeEndpoint
            properties:
              ackResourceMetadata:
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  region:
                    type: string
                type: object
              acmeEndpointARN:
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              endpointURL:
                type: string
              failureReason:
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
