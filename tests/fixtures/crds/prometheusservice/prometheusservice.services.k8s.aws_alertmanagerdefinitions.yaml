# Minimal stub CRD for prometheusservice.services.k8s.aws/AlertManagerDefinition v1alpha1.
# Fields: only those referenced by managedprometheusalertmanagerdefinition.aws.kropath.run RGD.
# Source: docs/crd-cache/aws/prometheusservice-v1.6.1.md
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: alertmanagerdefinitions.prometheusservice.services.k8s.aws
spec:
  group: prometheusservice.services.k8s.aws
  names:
    kind: AlertManagerDefinition
    listKind: AlertManagerDefinitionList
    plural: alertmanagerdefinitions
    singular: alertmanagerdefinition
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertManagerDefinition is the Schema for the AlertManagerDefinitions API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: AlertManagerDefinitionSpec defines the desired state of AlertManagerDefinition
            properties:
              configuration:
                description: Alert manager definition content in YAML or JSON format.
                type: string
              workspaceID:
                description: ID of the target workspace. Immutable once set.
                type: string
            required:
            - configuration
            type: object
          status:
            description: AlertManagerDefinitionStatus defines the observed state of AlertManagerDefinition
            properties:
              ackResourceMetadata:
                description: ACK resource metadata
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  partition:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              statusCode:
                description: Current status of the alert manager (CREATING, ACTIVE, etc.).
                type: string
              statusReason:
                description: Human-readable failure reason.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
