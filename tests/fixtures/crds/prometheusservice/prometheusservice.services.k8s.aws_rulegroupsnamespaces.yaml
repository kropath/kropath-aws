# Minimal stub CRD for prometheusservice.services.k8s.aws/RuleGroupsNamespace v1alpha1.
# Fields: only those referenced by managedprometheusrulegroupsnamespace.aws.kropath.run RGD.
# Source: docs/crd-cache/aws/prometheusservice-v1.6.1.md
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: rulegroupsnamespaces.prometheusservice.services.k8s.aws
spec:
  group: prometheusservice.services.k8s.aws
  names:
    kind: RuleGroupsNamespace
    listKind: RuleGroupsNamespaceList
    plural: rulegroupsnamespaces
    singular: rulegroupsnamespace
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RuleGroupsNamespace is the Schema for the RuleGroupsNamespaces API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: RuleGroupsNamespaceSpec defines the desired state of RuleGroupsNamespace
            properties:
              name:
                description: Name of the rule groups namespace. Required, immutable.
                type: string
              configuration:
                description: Prometheus alerting and recording rules in YAML format.
                type: string
              workspaceID:
                description: ID of the target workspace. Immutable once set.
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Key-value tags.
                type: object
            required:
            - name
            - configuration
            type: object
          status:
            description: RuleGroupsNamespaceStatus defines the observed state of RuleGroupsNamespace
            properties:
              ackResourceMetadata:
                description: ACK resource metadata
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  partition:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              status:
                description: Lifecycle status (nested object)
                properties:
                  statusCode:
                    description: Current status (CREATING, ACTIVE, etc.)
                    type: string
                  statusReason:
                    description: Failure reason when statusCode indicates failure.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
