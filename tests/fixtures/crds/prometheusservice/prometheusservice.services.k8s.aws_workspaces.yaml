# Minimal stub CRD for prometheusservice.services.k8s.aws/Workspace v1alpha1.
# Fields: only those referenced by managedprometheusworkspace.aws.kropath.run RGD.
# Source: docs/crd-cache/aws/prometheusservice-v1.6.1.md
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: workspaces.prometheusservice.services.k8s.aws
spec:
  group: prometheusservice.services.k8s.aws
  names:
    kind: Workspace
    listKind: WorkspaceList
    plural: workspaces
    singular: workspace
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workspace is the Schema for the Workspaces API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: WorkspaceSpec defines the desired state of Workspace
            properties:
              alias:
                description: Human-readable alias for the workspace.
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Key-value tags to associate with the workspace.
                type: object
            type: object
          status:
            description: WorkspaceStatus defines the observed state of Workspace
            properties:
              ackResourceMetadata:
                description: ACK resource metadata
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  partition:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              status:
                description: Workspace lifecycle status
                properties:
                  statusCode:
                    description: Workspace state (CREATING, ACTIVE, etc.)
                    type: string
                type: object
              workspaceID:
                description: Unique workspace identifier assigned by AWS.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
