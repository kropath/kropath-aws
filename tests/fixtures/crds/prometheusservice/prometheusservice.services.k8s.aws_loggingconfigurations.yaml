# Minimal stub CRD for prometheusservice.services.k8s.aws/LoggingConfiguration v1alpha1.
# Fields: only those referenced by managedprometheusloggingconfiguration.aws.kropath.run RGD.
# Source: docs/crd-cache/aws/prometheusservice-v1.6.1.md
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: loggingconfigurations.prometheusservice.services.k8s.aws
spec:
  group: prometheusservice.services.k8s.aws
  names:
    kind: LoggingConfiguration
    listKind: LoggingConfigurationList
    plural: loggingconfigurations
    singular: loggingconfiguration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoggingConfiguration is the Schema for the LoggingConfigurations API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: LoggingConfigurationSpec defines the desired state of LoggingConfiguration
            properties:
              logGroupARN:
                description: ARN of CloudWatch log group. Required.
                type: string
              workspaceID:
                description: ID of the target workspace. Immutable once set.
                type: string
            required:
            - logGroupARN
            type: object
          status:
            description: LoggingConfigurationStatus defines the observed state of LoggingConfiguration
            properties:
              ackResourceMetadata:
                description: ACK resource metadata
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  partition:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              statusCode:
                description: Current status (CREATING, ACTIVE, etc.).
                type: string
              statusReason:
                description: Failure reason when statusCode indicates failure.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
