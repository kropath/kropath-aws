---
# Minimal CRD fixture for bedrock.services.k8s.aws/InferenceProfile.
# Source: kropath-core/docs/crd-cache/aws/bedrock-v1.3.1.md
# Used by kro to resolve schema when compiling BedrockInferenceProfile RGD.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: inferenceprofiles.bedrock.services.k8s.aws
spec:
  group: bedrock.services.k8s.aws
  names:
    kind: InferenceProfile
    listKind: InferenceProfileList
    plural: inferenceprofiles
    singular: inferenceprofile
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceProfile is the Schema for the InferenceProfiles API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: InferenceProfileSpec defines the desired state of InferenceProfile
            properties:
              inferenceProfileName:
                description: A unique name for the cross-region inference profile.
                type: string
              modelSource:
                description: The foundation model or cross-region inference profile that the inference profile copies.
                properties:
                  copyFrom:
                    description: The ARN of the model or cross-region inference profile to copy.
                    type: string
                type: object
              description:
                description: A description of the inference profile. Immutable after creation.
                type: string
              tags:
                description: A list of tags to attach to the inference profile.
                items:
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
            required:
            - inferenceProfileName
            - modelSource
            type: object
          status:
            description: InferenceProfileStatus defines the observed state of InferenceProfile
            properties:
              ackResourceMetadata:
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              inferenceProfileID:
                description: The unique identifier of the inference profile.
                type: string
              inferenceProfileARN:
                description: The ARN of the inference profile.
                type: string
              status:
                description: The status of the inference profile.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
