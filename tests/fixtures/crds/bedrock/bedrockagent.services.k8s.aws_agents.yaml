---
# Minimal CRD fixture for bedrockagent.services.k8s.aws/Agent.
# Source: kropath-core/docs/crd-cache/aws/bedrockagent-v1.3.1.md
# Used by kro to resolve schema when compiling BedrockAgent RGD.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: agents.bedrockagent.services.k8s.aws
spec:
  group: bedrockagent.services.k8s.aws
  names:
    kind: Agent
    listKind: AgentList
    plural: agents
    singular: agent
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Agent is the Schema for the Agents API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: AgentSpec defines the desired state of Agent
            properties:
              agentName:
                description: A name for the agent that you create. Immutable.
                type: string
              foundationModel:
                description: The foundation model to use for the agent.
                type: string
              guardrailConfiguration:
                description: The guardrail configuration to attach to the agent.
                properties:
                  guardrailIdentifier:
                    type: string
                  guardrailVersion:
                    type: string
                type: object
              idleSessionTTLInSeconds:
                description: The number of seconds for which Amazon Bedrock keeps information about a user's conversation with the agent.
                format: int64
                type: integer
              agentResourceRoleARN:
                description: The ARN of the IAM role with permissions to invoke API operations on the agent.
                type: string
              instruction:
                description: Instructions that tell the agent what it should do and how it should interact with users.
                type: string
              customerEncryptionKeyARN:
                description: The ARN of the KMS key with which to encrypt the agent.
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Any tags that you want to attach to the agent.
                type: object
              memoryConfiguration:
                description: Memory configuration for the agent.
                properties:
                  enabledMemoryTypes:
                    items:
                      type: string
                    type: array
                  storageDays:
                    format: int64
                    type: integer
                type: object
            required:
            - agentName
            type: object
          status:
            description: AgentStatus defines the observed state of Agent
            properties:
              ackResourceMetadata:
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              agentID:
                description: The unique identifier of the agent.
                type: string
              agentStatus:
                description: The status of the agent.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
