---
# Minimal CRD fixture for bedrockagentcorecontrol.services.k8s.aws/Harness.
# Source: kropath-core/docs/crd-cache/aws/bedrockagentcorecontrol-v1.14.0.md
# Used by kro to resolve schema when compiling BedrockHarness RGD.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: harnesses.bedrockagentcorecontrol.services.k8s.aws
spec:
  group: bedrockagentcorecontrol.services.k8s.aws
  names:
    kind: Harness
    listKind: HarnessList
    plural: harnesses
    singular: harness
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Harness is the Schema for the Harnesses API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: HarnessSpec defines the desired state of Harness
            properties:
              harnessName:
                description: The name of the harness. Immutable after creation.
                type: string
              maxIterations:
                description: The maximum number of iterations for the harness.
                format: int64
                type: integer
              maxTokens:
                description: The maximum number of tokens for the harness.
                format: int64
                type: integer
              timeoutSeconds:
                description: The timeout in seconds for the harness.
                format: int64
                type: integer
              executionRoleARN:
                description: The ARN of the IAM role to use for execution.
                type: string
              model:
                description: The model identifier to use with the harness.
                type: string
              systemPrompt:
                description: The system prompt for the harness.
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags to attach to the harness.
                type: object
            required:
            - harnessName
            type: object
          status:
            description: HarnessStatus defines the observed state of Harness
            properties:
              ackResourceMetadata:
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              harnessID:
                description: The unique identifier of the harness.
                type: string
              harnessStatus:
                description: The status of the harness.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
