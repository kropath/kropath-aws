---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: tablebuckets.s3tables.services.k8s.aws
spec:
  group: s3tables.services.k8s.aws
  names:
    kind: TableBucket
    listKind: TableBucketList
    plural: tablebuckets
    singular: tablebucket
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TableBucket is the Schema for the S3 Tables TableBuckets API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: TableBucketSpec defines the desired state of TableBucket.
            properties:
              encryptionConfiguration:
                description: Server-side encryption configuration for the table bucket.
                properties:
                  kmsKeyARN:
                    description: ARN of the KMS key for SSE-KMS encryption.
                    type: string
                  kmsKeyRef:
                    description: ACK reference wrapper for the KMS key ARN.
                    properties:
                      from:
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                  sseAlgorithm:
                    description: Server-side encryption algorithm (AES256 or aws:kms).
                    type: string
                type: object
              maintenanceConfiguration:
                description: Maintenance configuration for the table bucket.
                properties:
                  icebergCompaction:
                    description: Automatic compaction settings for Iceberg tables.
                    properties:
                      settings:
                        properties:
                          targetFileSizeMB:
                            format: int64
                            type: integer
                        type: object
                      status:
                        type: string
                    type: object
                  icebergUnreferencedFileRemoval:
                    description: Unreferenced file removal settings for Iceberg tables.
                    properties:
                      settings:
                        properties:
                          nonCurrentDays:
                            format: int64
                            type: integer
                          unreferencedDays:
                            format: int64
                            type: integer
                        type: object
                      status:
                        type: string
                    type: object
                type: object
              name:
                description: Table bucket name. Required.
                type: string
              policy:
                description: Resource-based policy document for the table bucket.
                type: string
              storageClassConfiguration:
                description: Storage class configuration for the table bucket.
                properties:
                  storageClass:
                    description: Storage class to use for the table bucket.
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                description: Tags to assign to the table bucket.
                type: object
            required:
            - name
            type: object
          status:
            description: TableBucketStatus defines the observed state of TableBucket.
            properties:
              ackResourceMetadata:
                description: All CRs managed by ACK have a common Status.ACKResourceMetadata member.
                properties:
                  arn:
                    description: ARN is the Amazon Resource Name for the resource.
                    type: string
                  ownerAccountID:
                    description: OwnerAccountID is the AWS Account ID of the account that owns the backend AWS service API resource.
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                description: All CRS managed by ACK have a common Status.Conditions member.
                items:
                  description: Condition is the common struct used by all CRDs managed by ACK service controllers.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
