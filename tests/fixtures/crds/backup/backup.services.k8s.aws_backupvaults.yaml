---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: backupvaults.backup.services.k8s.aws
spec:
  group: backup.services.k8s.aws
  names:
    kind: BackupVault
    listKind: BackupVaultList
    plural: backupvaults
    singular: backupvault
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackupVault is the Schema for the BackupVaults API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: BackupVaultSpec defines the desired state of BackupVault.
            properties:
              encryptionKeyARN:
                description: ARN of the KMS key used for server-side encryption. Immutable.
                type: string
              encryptionKeyRef:
                description: ACK reference wrapper for the KMS key ARN.
                properties:
                  from:
                    description: AWSResourceReference describes a reference to an
                      AWS resource.
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    type: object
                type: object
              name:
                description: Vault name. Required, immutable.
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags to assign to the backup vault.
                type: object
            required:
            - name
            type: object
          status:
            description: BackupVaultStatus defines the observed state of BackupVault.
            properties:
              ackResourceMetadata:
                description: Standard ACK resource metadata.
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  partition:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                description: ACK condition list.
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              creationDate:
                description: Vault creation timestamp.
                type: string
              lockDate:
                description: When Vault Lock configuration can no longer be changed.
                type: string
              locked:
                description: Whether Vault Lock is active.
                type: boolean
              maxRetentionDays:
                description: Maximum retention period in days.
                format: int64
                type: integer
              minRetentionDays:
                description: Minimum retention period in days.
                format: int64
                type: integer
              numberOfRecoveryPoints:
                description: Count of recovery points stored.
                format: int64
                type: integer
              vaultType:
                description: Type of vault.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
