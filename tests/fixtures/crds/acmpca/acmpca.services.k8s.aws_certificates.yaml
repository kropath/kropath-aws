---
# Minimal CRD fixture for acmpca.services.k8s.aws/Certificate (private cert).
# Source: kropath-core/docs/crd-cache/aws/acmpca-controller-v1.4.1.md
# Used by kro to resolve schema when compiling ACMPrivateCertificate RGD.
# Note: validity.type is named "type" in the provider CRD. The RGD template
# also uses "type:" as the key — kro does literal field lookup.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: certificates.acmpca.services.k8s.aws
spec:
  group: acmpca.services.k8s.aws
  names:
    kind: Certificate
    listKind: CertificateList
    plural: certificates
    singular: certificate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Certificate is the Schema for the ACMPCA Certificates API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: CertificateSpec defines the desired state
            properties:
              certificateAuthorityARN:
                type: string
              certificateOutput:
                description: SecretKeyReference for the output certificate
                properties:
                  key:
                    type: string
                  name:
                    type: string
                required:
                - key
                - name
                type: object
              signingAlgorithm:
                type: string
              templateARN:
                type: string
              validity:
                properties:
                  type:
                    type: string
                  value:
                    format: int64
                    type: integer
                type: object
            type: object
          status:
            description: CertificateStatus defines the observed state
            properties:
              ackResourceMetadata:
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  region:
                    type: string
                type: object
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
