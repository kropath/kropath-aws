# Minimal ACK CRD stub — fallback only, applied by tests/setup.sh ONLY when the real
# CRD is absent (e.g. ECR/GitHub-API unavailable). Snapshot of ram-controller v1.5.0
# with descriptions stripped. Never apply this over a live ACK CRD.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: resourceshares.ram.services.k8s.aws
spec:
  group: ram.services.k8s.aws
  names:
    kind: ResourceShare
    listKind: ResourceShareList
    plural: resourceshares
    singular: resourceshare
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              allowExternalPrincipals:
                type: boolean
              name:
                type: string
              permissionARNs:
                items:
                  type: string
                type: array
              permissionRefs:
                items:
                  properties:
                    from:
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                      type: object
                  type: object
                type: array
              principals:
                items:
                  type: string
                type: array
              resourceARNs:
                items:
                  type: string
                type: array
              sources:
                items:
                  type: string
                type: array
              tags:
                items:
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
            required:
            - name
            type: object
          status:
            properties:
              ackResourceMetadata:
                properties:
                  arn:
                    type: string
                  ownerAccountID:
                    type: string
                  partition:
                    type: string
                  region:
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                format: date-time
                type: string
              featureSet:
                type: string
              lastUpdatedTime:
                format: date-time
                type: string
              owningAccountID:
                type: string
              status:
                type: string
              statusMessage:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
