---
apiVersion: aws.kropath.run/v1alpha1
kind: ECSService
metadata:
  name: svc-lt-defaults
  namespace: ecsservice
spec:
  configRef: lt-defaults-policy
  clusterName: default-cluster
  taskDefinition: default-task:1
  desiredCount: 1
  deletionPolicy: retain
