apiVersion: iam.services.k8s.aws/v1alpha1
kind: Policy
metadata:
  name: default-tagged-policy
  namespace: default
spec:
  name: default-tagged-policy
  path: "/"
  tags:
    - key: team
      value: payments
