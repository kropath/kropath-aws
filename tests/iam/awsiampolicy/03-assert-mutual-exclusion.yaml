apiVersion: aws.kropath.run/v1alpha1
kind: AWSIAMPolicy
metadata:
  name: invalid-both
  namespace: awsiampolicy
status:
  validationError: "documentJSON and documentRef are mutually exclusive — set exactly one"
