apiVersion: iam.services.k8s.aws/v1alpha1
kind: Policy
metadata:
  name: labeled-policy
  namespace: default
  labels:
    kropath.run/environment: production
spec:
  name: labeled-policy
  path: "/"
  tags:
    - key: kropath.run/environment
      value: production
