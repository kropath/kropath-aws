apiVersion: aws.kropath.run/v1alpha1
kind: KropathConfig
metadata:
  name: baseline
  namespace: cascade
spec:
  mandatory:
    tags:
      cost-center: platform
    syncedLabels:
      managed-by: kropath
  defaults:
    tags:
      environment: test
