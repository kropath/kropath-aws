apiVersion: aws.kropath.run/v1alpha1
kind: IAMUser
metadata:
  name: inline-doc-ref-user
  namespace: iamuser
spec:
  configRef: user-general-policy
  policies:
    - inline:
        name: ec2-read
        documentRef: my-policy-doc
