apiVersion: kropath.run/v1alpha1
kind: AWSIAMRole
metadata:
  name: ref-role
  namespace: default
spec:
  configRef: role-general-policy
  type: generic
  trustPolicyRef: trust-doc
  inlinePolicies:
    - name: inline-read
      documentRef: inline-doc
