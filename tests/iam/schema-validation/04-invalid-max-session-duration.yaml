# Should be rejected: maxSessionDuration below the 900-second non-zero floor.
apiVersion: kropath.run/v1alpha1
kind: AWSIAMRole
metadata:
  name: invalid-max-session-duration
  namespace: default
spec:
  configRef: role-general-policy
  type: lambda
  maxSessionDuration: 899
