apiVersion: kropath.run/v1alpha1
kind: AWSIAMGroup
metadata:
  name: inline-ref-group
  namespace: awsiamgroup
spec:
  configRef: general-policy
  inlinePolicies:
    - name: s3-list
      documentRef: group-inline-doc
