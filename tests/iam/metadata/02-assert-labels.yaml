# Partial-match assertion: standard managed-by and instance labels must exist
# on the generated iam.services.k8s.aws/Role child resource.
apiVersion: iam.services.k8s.aws/v1alpha1
kind: Role
metadata:
  name: my-lambda-role
  namespace: default
  labels:
    app.kubernetes.io/managed-by: kro
    app.kubernetes.io/instance: my-lambda-role
