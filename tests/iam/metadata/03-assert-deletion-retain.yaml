# Partial-match assertion: default deletionPolicy=retain must produce the ACK annotation.
apiVersion: iam.services.k8s.aws/v1alpha1
kind: Role
metadata:
  name: my-lambda-role
  namespace: default
  annotations:
    services.k8s.aws/deletion-policy: retain
