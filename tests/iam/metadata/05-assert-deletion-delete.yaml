# Partial-match assertion: explicit deletionPolicy=delete must produce delete on the ACK annotation.
apiVersion: iam.services.k8s.aws/v1alpha1
kind: Role
metadata:
  name: my-lambda-role
  namespace: metadata
  annotations:
    services.k8s.aws/deletion-policy: delete
