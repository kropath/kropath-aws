# Partial-match assertion: ownerReferences must point back to the AWSIAMRole parent.
apiVersion: iam.services.k8s.aws/v1alpha1
kind: Role
metadata:
  name: my-lambda-role
  namespace: metadata
  ownerReferences:
    - apiVersion: kropath.run/v1alpha1
      kind: AWSIAMRole
      name: my-lambda-role
      controller: true
      blockOwnerDeletion: true
