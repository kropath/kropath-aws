# Assert: effectiveConfig is populated with merged values.
apiVersion: aws.kropath.run/v1alpha1
kind: AWSIAMConfig
metadata:
  name: general-policy
  namespace: awsiamconfig
status:
  effectiveConfig:
    defaults:
      maxSessionDurationSeconds: 3600
      namingTemplate: "{namespace}-{name}"
    aws:
      accountId: "123456789012"
      region: "us-east-1"
