# Assert: general-policy CR was accepted with correct field values (AC-1).
apiVersion: aws.kropath.run/v1alpha1
kind: WAFConfig
metadata:
  name: general-policy
  namespace: wafconfig
spec:
  mandatory:
    scope: ""
    defaultAction: ""
    cloudWatchMetricsEnabled: false
    sampledRequestsEnabled: false
    namingTemplate: ""
  defaults:
    scope: "REGIONAL"
    defaultAction: "block"
    cloudWatchMetricsEnabled: true
    sampledRequestsEnabled: true
    namingTemplate: "{namespace}-{name}"
