# AC-4: tags in both mandatory AND defaults is valid (maps merge, not mutually exclusive).
apiVersion: aws.kropath.run/v1alpha1
kind: SageMakerConfig
metadata:
  name: ac4-map-both-tiers
  namespace: sagemakerconfig
spec:
  mandatory:
    tags:
      env: prod
  defaults:
    tags:
      team: ml
