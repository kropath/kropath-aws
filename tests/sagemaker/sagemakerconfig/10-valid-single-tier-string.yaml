# AC-8: SageMakerConfig with only mandatory.rootAccess set is accepted (single tier, no conflict).
apiVersion: aws.kropath.run/v1alpha1
kind: SageMakerConfig
metadata:
  name: ac8-single-tier-string
  namespace: sagemakerconfig
spec:
  mandatory:
    rootAccess: "Disabled"
