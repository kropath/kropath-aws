# AC-6 assert: the general-policy CR has the expected selector label.
apiVersion: aws.kropath.run/v1alpha1
kind: SageMakerConfig
metadata:
  name: general-policy
  namespace: sagemakerconfig
  labels:
    aws.kropath.run/resource-name: general-policy
