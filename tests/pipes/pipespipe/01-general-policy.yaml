---
# General-policy PipesConfig in kro-system (re-applied here to ensure it exists).
# This is the same as tests/pipes/pipesconfig/08-general-policy.yaml.
apiVersion: aws.kropath.run/v1alpha1
kind: PipesConfig
metadata:
  name: general-policy
  namespace: kro-system
  labels:
    aws.kropath.run/resource-name: general-policy
spec:
  mandatory:
    desiredState: ""
    namingTemplate: ""
    tags: {}
    syncedLabels: {}
    syncedAnnotations: {}
  defaults:
    desiredState: "running"
    namingTemplate: "{namespace}-{name}"
    tags: {}
    syncedLabels: {}
    syncedAnnotations: {}
