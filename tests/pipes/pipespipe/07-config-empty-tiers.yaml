---
# PipesConfig with all tiers empty (no desiredState anywhere) — used for AC-4
apiVersion: aws.kropath.run/v1alpha1
kind: PipesConfig
metadata:
  name: config-empty-tiers
  namespace: pipespipe
  labels:
    aws.kropath.run/resource-name: config-empty-tiers
spec:
  mandatory:
    desiredState: ""
    namingTemplate: ""
    tags: {}
    syncedLabels: {}
    syncedAnnotations: {}
  defaults:
    desiredState: ""
    namingTemplate: "{namespace}-{name}"
    tags: {}
    syncedLabels: {}
    syncedAnnotations: {}
