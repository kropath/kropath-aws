---
# PipesConfig for AC-21 — syncedLabels dual-write test
apiVersion: aws.kropath.run/v1alpha1
kind: PipesConfig
metadata:
  name: config-synced-labels
  namespace: pipespipe
  labels:
    aws.kropath.run/resource-name: config-synced-labels
spec:
  mandatory:
    desiredState: ""
    namingTemplate: ""
    tags: {}
    syncedLabels: {}
    syncedAnnotations: {}
  defaults:
    desiredState: "running"
    namingTemplate: "{namespace}-{name}"
    tags: {}
    syncedLabels:
      env: prod
    syncedAnnotations: {}
