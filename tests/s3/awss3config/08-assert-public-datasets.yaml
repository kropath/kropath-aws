# Assert: the public-datasets profile intentionally leaves blockPublicAccess disabled.
apiVersion: kropath.run/v1alpha1
kind: AWSS3Config
metadata:
  name: public-datasets
  namespace: kro-system
spec:
  mandatory:
    blockPublicAccess: false
  defaults:
    blockPublicAccess: true
